# Re-audit Checkpoint #2 — 2026-05-19

**Baseline:** Phase 5 tail edge. Cumulative diff = slice 8 only (BA-F4 transactional audit-log refactor + BA-F11 + FG2-M6 + SA-F4).
**Prior baseline:** `docs/audit/hex-audit-2026-05-18.md` (the v1.1.3-era full project audit).
**Workflow source:** `memory/workflow-hex-audit.md` (the named hex-audit workflow).
**Why this checkpoint:** per the phase table, Phases 2 and 5 are the named re-audit slots. Phase 5 introduced ~250 lines of new production code (new repo contract + retention loop + transactional refactor across 13 call sites) — non-trivial diff that warrants a fresh 4-lens read.

## Scope

Phase 5 diff (slice 8). Files touched:

**App (10 files):**
- `app/repositories/audit_log.py` — full rewrite, new dual-contract `append(session, ...)` + `append_standalone(...)` + new `delete_older_than(...)` + `run_wal_checkpoint(...)`
- `app/config.py` — new `audit_log_retention_days: Annotated[int, Field(ge=30, le=3650)] = 365`
- `app/api/targets.py` — 6 call sites (create/update/delete + credential_set/revealed/cleared) refactored to in-session append before commit
- `app/api/settings_api.py` — 3 call sites
- `app/api/security_api.py` — 2 call sites (passphrase_rotated moved INSIDE rewrap `session.begin()` block; session_revoked moved before commit)
- `app/api/auth.py` — 2 call sites (password_rehashed merged into one txn; password_changed audit-before-commit + drop `contextlib.suppress`)
- `app/api/api_tokens_api.py` — 2 call sites
- `app/fanout/supervisor.py` — `_audit` helper uses `append_standalone`; `_wipe_per_session_credentials` merged into single `session.begin()` block
- `app/main.py` — `app_started` lifespan emit (suppress removed, fatal on failure); `_kdf_salt_previous_cleanup_once` (switched to `append_standalone`); `_ingest_key_previous_cleanup_loop` (merged into one txn); NEW `_audit_log_retention_loop`
- `docs/architecture/ADR-0007-persistence.md` — new "Audit-log durability and transactional shape" section + audit-scope correction (SA-F4)

**Tests (4 files):**
- `tests/db/test_repositories.py::TestAuditLogRepository` — class rewritten (3 → 7 tests)
- `tests/db/test_audit_log_retention.py` — NEW (4 tests)
- `tests/test_kdf_salt_previous_cleanup.py` — `_FakeAuditRepo.append` → `append_standalone`
- `tests/api/test_phase9_endpoints.py` — 2 direct-seeding sites switched to `append_standalone`

## Lens decisions for this checkpoint

| Lens | Decision | Reason |
|---|---|---|
| Backend Architect | RUN | Auth/crypto adjacent; new repo contract + transactional model |
| Software Architect | RUN | ADR-0007 changes; audit-event vocabulary; architectural shape |
| UX Architect | SKIP | Zero frontend diff — no surface to audit |
| UI Designer | SKIP | Zero frontend diff — no surface to audit |
| Code-reviewer | RUN | Full diff sweep; verify slice-8 reviewer pass invariants |
| Footgun Hunter (Rule №6) | RUN | Retention loop is new long-running async + state-machine surface; runtime-behavioral footguns possible |

Four agents, all running in background. UX/UI explicitly out of scope per the cold-resume tail in `SESSION_HANDOFF.md`.

## Status legend

- `[x]` **APPLIED** — fix landed in working tree.
- `[ ]` **OPEN** — not yet touched.
- `[~]` **DEFERRED** — intentionally postponed (e.g., needs separate ADR / standalone PR / outside Rule №1 scope).
- `[-]` **WONTFIX** — agent flagged "leave it" or cost > benefit.
- `[?]` **NEEDS RECOVERY** — transcript truncated.

## Findings

(Findings will be appended below as agents return. Per workflow: persist on receipt, not at end-of-session.)

### Backend Architect

Agent `a53267590f13a925e` — 8 findings (0 Critical / 2 High / 5 Medium / 1 Low).

- [-] **BA2-F1** High — ANALYZED, NOT FIXED. The architect's proposed reorder (decrypt AFTER audit) does not actually improve forensic accuracy under FastAPI's response model. With current order (decrypt → audit-append → commit → return): commit failure rolls back the audit row AND the `return` statement never executes — plaintext never serialised to wire. With proposed order (audit-append → decrypt → commit → return): same outcome — commit failure rolls back audit, decrypt failure rolls back audit, return never executes. Both orderings yield identical observable behavior. The "operator briefly saw plaintext in their browser before 500'd" angle doesn't apply — FastAPI sends the response body only on successful return, not on exception. No-op fix; documented for future reference. [targets.py:406-435](app/api/targets.py#L406), [settings_api.py:192-207](app/api/settings_api.py#L192).

- [x] **BA2-F2** High — `passphrase_rotated` audit row commits BEFORE the salt-file rename. If the rename raises OSError (`_logger.critical("passphrase_rotation_orphaned")` path), the audit row says "rotated successfully" but the on-disk salt is stale. On next boot, KDF derives OLD KEK from OLD salt against NEW ciphertexts → cannot decrypt. Forensic record LIES about reality. [security_api.py:318-356](app/api/security_api.py#L318). **Proposed fix:** emit `passphrase_rotation_started` INSIDE the rewrap txn (committed atomically); emit `passphrase_rotated` via `append_standalone(checkpoint=False)` AFTER the rename AND in-process key swap succeed; on orphan path emit `passphrase_rotation_orphaned`. Audit-as-truth restored.

- [x] **BA2-F3** Medium — Retention loop's `except Exception: continue` masks deteriorating-DB conditions indefinitely. Disk-full / corrupted-page / schema-drift → loop hits the warning path every 6h forever, WAL grows unbounded (checkpoint fails too — same DB), operator's only signal is one warning line per 6h. No escalation, no `/readyz` flip. [main.py:743-748](app/main.py#L743). **Proposed fix:** track `_consecutive_failures` local; after `N=3` (≥18h) upgrade to `_logger.critical` AND set `app.state.audit_log_retention_degraded = True` flag that `/readyz` reads to flip readiness.

- [x] **BA2-F4** Medium — `append_standalone` defaults `checkpoint=True`, but supervisor `_audit` helper (called from `worker_failed_open`, `run_started`, `run_stopped` — potentially several times per second during a flapping run) inherits per-append `wal_checkpoint(TRUNCATE)`. This is the EXACT pattern BA-F4 aimed to remove from the canonical path; now revived for the supervisor's hot path. [audit_log.py:140-181](app/repositories/audit_log.py#L140), [supervisor.py:879-906](app/fanout/supervisor.py#L879). **Proposed fix:** supervisor `_audit` helper passes `checkpoint=False`; retention loop's `run_wal_checkpoint` already covers WAL hygiene. Restores BA-F4 invariant. Lifespan `app_started` keeps `checkpoint=True` (fires exactly once per boot).

- [x] **BA2-F5** Medium STRONG (with FG3-F5) — Applied slice 10. New `_WIPE_BATCH_SIZE=5` constant in `supervisor.py`. `_wipe_per_session_credentials` iterates in 5-target chunks; each chunk opens its own `session.begin()` block (delete + audit for ≤5 targets) and commits. Write-lock duration is now O(batch_size) — concurrent OBS `on_publish` webhook inserts no longer wait on the entire wipe. Per-target atomicity preserved within a batch; cross-target atomicity was never required (each VK session credential is independent). [supervisor.py:111-121,857-880](app/fanout/supervisor.py#L111)

- [x] **BA2-F6** Medium — Retention loop has zero observability on idle ticks. `audit_log_retention_run` row only lands on non-empty deletes; on young/quiet deployments the row is absent for months. The retention-degraded scenario (BA2-F3) is indistinguishable from "nothing to delete yet". [main.py:729-757](app/main.py#L729). **Proposed fix:** emit a structured-log line `audit_log_retention_tick` at INFO on every tick with `rows_deleted` (may be 0), `cutoff`, `retention_days`. Gives operators a `journalctl | grep audit_log_retention_tick | tail -5` heartbeat. Do NOT add idle audit-of-audit rows (WAL spam).

- [x] **BA2-F7** Medium — Applied slice 9 (DOC, not migration). ADR-0007 §"Audit-log durability and transactional shape" gains "ADR drift acknowledgement" sub-paragraph: explicitly names the env-only-vs-settings-row drift, names the intentional reason for v1.1.4 (operator-restart requirement, no hot-tunable trim that could fall below the previous tick's deletion horizon), and names the future-amendment trigger (compliance request → migrate to settings row with env-var seeding on first boot only). The env-only choice is now documented intent, no longer silent ADR drift. [ADR-0007:74-100](docs/architecture/ADR-0007-persistence.md#L74)

- [x] **BA2-F8** Low — Applied slice 10. `_run_wal_checkpoint` signature simplified to `(*, reason: str)`. The `wal_checkpoint_truncate_busy` structured-log emits `reason` only; removed the misleading `audit_event_type` / `audit_id` fields that suggested per-row correlation the checkpoint cannot actually provide. `append_standalone` callers pass `reason=f"append_standalone:{event_type}"`. [audit_log.py:178-250](app/repositories/audit_log.py#L178)

**Things checked OK:** dual-contract abstraction (right shape); `delete_older_than` primitive (clean); `_ingest_key_previous_cleanup_loop` merge (correct); `app_started` fatal-on-failure semantic (correct); `audit_log_retention_days` bounds enforced at config-load (fail-fast intact); ADR-0007 audit-log scope amendment (resolves long-standing drift); slice-3 lock ordering preserved; `delete_target` FK handling (target_id in data, not column); `password_rehashed` background-task merge (correct); `list_app_starts` unchanged.

### Software Architect

Agent `a140e9ad14e9184b7` — 11 findings (0 Critical / 3 High / 6 Medium / 2 Low). All docs-only (no code changes proposed).

- [x] **SA2-F1** High — ADR-0005 §"Audit" enumeration replaced with one-liner pointing at ADR-0007 §"Audit log scope" as the single source of truth. Applied slice 9. [ADR-0005:249-261](docs/architecture/ADR-0005-auth-model.md#L249)

- [x] **SA2-F2** High — ADR-0007 §"Engine configuration" rewritten: the loss window is now bounded by the retention cadence (6 h busy / 24 h idle); the trade is named explicitly and cross-refs §Consequences. Applied slice 9. [ADR-0007:50-60](docs/architecture/ADR-0007-persistence.md#L50)

- [x] **SA2-F3** High — ADR-0007 §"Audit log scope" gains a 5th "System maintenance lifecycle" bucket enumerating `audit_log_retention_run` (eternal forensic record), `kdf_salt_previous_cleared`, `ingest_key_previous_cleared`, `app_started`. Applied slice 9. [ADR-0007:131-145](docs/architecture/ADR-0007-persistence.md#L131)

- [x] **SA2-F4** Medium — Resolved via ADR-0013 (new). ADR-0013 explicitly adopts "outcome in data, never in event_type suffix" and cites `audit_log_retention_run` as the reference pattern with full rationale (catalogue cardinality, consumer simplicity, analytical-query OR-free). Applied slice 9. [ADR-0013](docs/architecture/ADR-0013-audit-event-vocabulary.md)

- [x] **SA2-F5** Medium — ADR-0007 §"Open questions / revisit triggers" gains a third revisit trigger ("Dual-contract audit repo") naming the `>3 net-new standalone call sites` threshold (excluding lifespan / supervisor / post-side-effect amendments). Applied slice 9. [ADR-0007:200-215](docs/architecture/ADR-0007-persistence.md#L200)

- [x] **SA2-F6** Medium — ADR-0007 §"Audit-log durability and transactional shape" gains "Why audit-of-audit and not a separate `audit_log_retention_runs` table?" sub-paragraph; rejected alternative is named (signal-to-noise concern handled by retention-DELETE exclusion, not by relocation). Applied slice 9. [ADR-0007:60-90](docs/architecture/ADR-0007-persistence.md#L60)

- [x] **SA2-F7** Medium — ADR-0007 §"Consequences" gains "audit-log retention is tunable" (Easier) and "audit-write failures now propagate as 500/503" (Harder) bullets; also "OS-power-loss window grew from microseconds to one retention tick" (Harder). Applied slice 9. [ADR-0007:182-198](docs/architecture/ADR-0007-persistence.md#L182)

- [x] **SA2-F8** Medium — Phase 9 design memo §M gains explicit slice-8 amendment note covering BOTH the `contextlib.suppress` removal (audit-write failure on boot is now fatal) AND the `checkpoint=False` change that closes the WAL boot-race (FG3-F1). Applied slice 9. [phase-9-design-memo.md:625-645](docs/architecture/phase-9-design-memo.md#L625)

- [x] **SA2-F9** Medium — Phase 6 design memo §Q7 gains slice-8 amendment note: the BackgroundTask now opens one session, merges hash-update + audit row in the same `session.begin()` block, then commits once. Applied slice 9. [phase-6-design-memo.md:235-260](docs/architecture/phase-6-design-memo.md#L235)

- [x] **SA2-F10** Low — ADR-0007 §"Audit-log durability and transactional shape" vocabulary unified — explicitly names "one physical transaction" alongside `session.begin()` block on first mention; subsequent mentions use "the same physical transaction". Applied slice 9. [ADR-0007:54-90](docs/architecture/ADR-0007-persistence.md#L54)

- [x] **SA2-F11** Low — ADR-0006 §"Rejected alternatives" Sqlcipher bullet now cross-refs ADR-0007 §"Audit log scope" + names the specific events (passphrase_rotation_started/rotated/orphaned, credential_set/revealed/cleared, kdf_salt_previous_cleared) the per-field/AAD approach enables. Applied slice 9. [ADR-0006:203-216](docs/architecture/ADR-0006-secret-encryption.md#L203)

**Things checked OK:** reversibility intact (rows from `append` and `append_standalone` are SQL-identical); layering correct (`SessionDep` to audit is right shape); domain coherence (audit-of-audit defensible at scale); ADR-0001/0008/0010/0011 no drift; ADR-0009 future-possibility text unaffected; 6h cadence docstring honestly trades coarseness; `append_standalone(checkpoint=False)` parameter clean; 30-3650 day bounds honor forensic intent.

### Code-reviewer

Agent `a56fed3fd0aff3a5d` — 4 findings (0 Critical / 1 High / 2 Medium / 1 Low).

- [x] **CR2-Re-F1** High — `run_wal_checkpoint` fires OUTSIDE the retention loop's `try/except Exception` guard. If the checkpoint raises (transient SQLITE_BUSY on concurrent WAL reader, network blip on remote FS), the exception escapes the `while True` loop and **silently kills the retention loop permanently**. Future ticks never run, WAL grows unbounded, no audit retention ever fires again. Docstring claims "per-iteration failures are logged and the loop continues" — that invariant is BROKEN for checkpoint failures. [main.py:749-757](app/main.py#L749). Confidence 95. **Fix:** wrap each `run_wal_checkpoint` call in its own `try/except Exception` with `_logger.warning`.

- [x] **CR2-Re-F2** Medium — `last_idle_checkpoint = now` updated even when the preceding checkpoint raised. With CR2-Re-F1 fixed, the variable still needs to be moved INSIDE the try block (success branch only) — otherwise checkpoint failure days defer the next idle checkpoint another 24h despite checkpoint not having run. WAL growth can exceed the 24h bound. [main.py:749-757](app/main.py#L749). **Fix:** update `last_idle_checkpoint = now` only in success branch.

- [x] **CR2-Re-F3** Medium — Test gap: `_audit_log_retention_loop`'s checkpoint-failure-isolation and `last_idle_checkpoint` rollback are untested. The 4 retention tests cover delete+marker atomicity + WAL checkpoint happy path but NOT failure isolation. CR2-Re-F1 wouldn't have been caught by the test suite. [tests/db/test_audit_log_retention.py](tests/db/test_audit_log_retention.py). **Fix:** add a test that monkey-patches `repo.run_wal_checkpoint` to raise, runs two simulated loop bodies, asserts loop doesn't re-raise + `last_idle_checkpoint` not advanced.

- [x] **CR2-Re-F4** Low — Applied slice 10. New `_AsyncSessionmaker = async_sessionmaker[AsyncSession]` alias under `if TYPE_CHECKING:` in `app/main.py`. All five background loops (`_kdf_salt_previous_cleanup_once`, `_kdf_salt_previous_cleanup_loop`, `_ingest_key_previous_cleanup_loop`, `_audit_log_retention_once`, `_audit_log_retention_loop`) + `_read_ingest_key_now` now type the parameter as `_AsyncSessionmaker`. All 6 `# type: ignore[arg-type|operator]` removed; mypy 72/72 still clean. Zero runtime cost (TYPE_CHECKING-guarded). [main.py:81-95,596-970](app/main.py#L81)

**Things checked OK:** zero `contextlib.suppress` around audit calls (grep verified); `password_changed` audit-before-commit ordering correct; `append` docstring matches implementation (no checkpoint in path); supervisor wipe merged-txn atomicity confirmed; retention cutoff boundary `at < cutoff` is exclusive (rows at exact cutoff timestamp survive — correct); `AuditLogRepository` reuse across loop iterations has no per-iteration mutable state (no leak); SQLite WAL snapshot isolation prevents retention from deleting an uncommitted row; test fixture isolation (fresh DB per test); `_FakeAuditRepo.append_standalone` rename correct; `revoke_all_for_user` shares session object with audit append (atomic).

### Footgun Hunter (Rule №6)

Agent `a59924181eee31cec` — 9 findings (4 Critical / 3 High / 2 Medium).

- [x] **FG3-F1** Critical — Boot-loop on transient SQLite WAL contention at startup. `app_started` audit dropped `contextlib.suppress` (correct intent), but `append_standalone` runs `wal_checkpoint(TRUNCATE)`. If the WAL has a dirty checkpoint pending after unclean shutdown, the first checkpoint can raise `OperationalError: database is locked`. Boot fails → s6 restarts → same race → infinite boot loop. [main.py:375-382](app/main.py#L375). **Fix:** pass `checkpoint=False` for the startup row (retention loop will checkpoint on next tick), OR bounded retry (3 attempts, exponential 200ms/1s/5s backoff).

- [x] **FG3-F2** Critical — Retention loop infinite spin on persistent DB failure with no degraded-mode signal. `except Exception: continue` masks corrupt schema / disk-full / permission flips. Loop spins for process lifetime, ONE warning per 6h, retention floor silently NOT enforced (compliance posture broken). [main.py:723-748](app/main.py#L723). **Fix:** track consecutive-failure count; after N=5 (~30h) escalate to ERROR with traceback + surface degraded flag on `/api/about` or `/api/health` so the panel can show a banner. Sibling: `_kdf_salt_previous_cleanup_loop` and `_ingest_key_previous_cleanup_loop` same pattern. **STRONG-SIGNAL** — also raised by BA2-F3.

- [x] **FG3-F3** Critical — `passphrase_rotated` audit lies when post-commit salt-file rename fails. Audit row commits inside `session.begin()` block (BA-F4 atomicity), AFTER commit `new_path.replace(salt_path)` may OSError; audit says "rotated" but on-disk salt is OLD → next boot KDF derives OLD KEK against NEW ciphertexts → unbootable. [security_api.py:318-355](app/api/security_api.py#L318). **Fix:** emit `passphrase_rotation_started` INSIDE rewrap txn; emit `passphrase_rotated` via `append_standalone(checkpoint=False)` AFTER rename + key swap succeed; on orphan path emit `passphrase_rotation_orphaned`. **STRONG-SIGNAL** — also raised by BA2-F2.

- [x] **FG3-F4** Critical — Audit-of-audit row participates in retention, eventually consuming the floor itself. 6h cadence → 1460 retention rows/year. Low-activity deployment with 50 real audit rows/year → audit-log is 96.6% noise. When an aged retention row deletes itself, the next tick emits a NEW retention row (`deleted > 0`), cycle compounds. [main.py:732-742](app/main.py#L732). **Fix:** exclude `event_type = 'audit_log_retention_run'` from the `delete_older_than` predicate (those rows are eternal but tiny); OR write retention markers to a separate `audit_log_retention_runs` sibling table with its own bounded retention.

- [x] **FG3-F5** High STRONG (with BA2-F5) — Applied slice 10. Closed alongside BA2-F5 via the `_WIPE_BATCH_SIZE=5` chunked-txn fix. Write-lock per batch ≈ 10 statement turnarounds — well under `busy_timeout=5000ms` even on slow disks. OBS `on_publish` webhook can no longer wait on the entire wipe across 30+ VK targets.

- [x] **FG3-F6** High — `target_deleted` row leaves `target_id` column NULL, breaking `list_by_target` queries. FK is `ON DELETE SET NULL` (not REJECT — the original comment misread the constraint), so within ONE transaction the INSERT after DELETE would fail the immediate FK check (default `defer_foreign_keys=OFF`). Consequence: an auditor running `list_by_target(X)` for a deleted target sees `target_created/updated/credential_set` (all with `target_id=X`) and then NOTHING. The deletion is in `list_recent` only, identified by `data["target_id"]`. Silent forensic gap. [targets.py:270-281](app/api/targets.py#L270). **Fix:** `session.execute(text("PRAGMA defer_foreign_keys=1"))` for the txn, then pass `target_id` normally — at commit-time SET NULL fires, audit row persists with `target_id=NULL` but the row was meaningful at insert-time. OR: also index `data_json` for the target_id substring via JSON1.

- [x] **FG3-F7** High — `password_changed` audit failure leaks through the timing-floor `finally`. 1-second floor in `finally` runs even when audit.append raises (DB locked). Client gets 500 with normal timing — INDISTINGUISHABLE from a successful change in client-side behavior. Password was NOT updated (BA-F4 merged txn rolls back), but user believes it changed; old password still works. UX + security-posture confusion. [auth.py:281-316](app/api/auth.py#L281). **Fix:** catch audit-specific exception, log critically, return 503 with `Retry-After` header + distinguishable error code (e.g., `PASSWORD_CHANGE_RETRY`) so SPA shows "Try again in a moment" rather than generic "Something went wrong".

- [-] **FG3-F8** Medium — (Resolved during agent investigation: pydantic int-coercion accepts `"30"` and rejects `"30.5"`; env values are always strings so `True → 1` not reachable. Moved to OK list by the agent.)

- [x] **FG3-F9** Medium — Applied slice 10 (chose lower-effort option). `_audit_log_retention_loop` runs one `_audit_log_retention_once(...)` tick on entry BEFORE the first sleep. On a fresh boot the delete is likely 0 rows + one WAL checkpoint; bounds the worst case for a restart-heavy deployment where the 6h cadence would otherwise never fire. State table persistence still available as a future option if the unconditional boot-tick proves too expensive. [main.py:874-918](app/main.py#L874)

- [x] **FG3-F10** Medium — Applied slice 10. Supervisor `_audit` helper wraps `append_standalone` in `try/except IntegrityError`; on FK violation re-emits with `target_id=None` and pushes the original id into `data` (alongside `fk_race: True` marker) — mirrors the `target_deleted` audit shape. Outer `except Exception:` around the fallback prevents recursion. Forensic record survives; FK violation no longer propagates up the supervisor's hot path. [supervisor.py:907-958](app/fanout/supervisor.py#L907)

**Things checked OK:** pydantic bounds correctly reject invalid input; retention loop context-manager protocol cleans session on exception; cutoff arithmetic uses tz-aware datetimes; SQLite single-writer with `try/except: continue` handles `OperationalError` cleanly; JSON blob size bounded; `_ingest_key_previous_cleanup_loop` sibling-safe (bool cleared, no fanout); `append_standalone(checkpoint=False)` threads flag correctly; `app_started` JSON bounded; TaskGroup cancel propagates; `_rehash_admin_password` background-task swallow is intentional and correct.

## Synthesis

**Total: 32 findings across 4 lenses** (4 Critical / 9 High / 15 Medium / 4 Low). Backend Architect: 8. Software Architect: 11 (docs-only). Code-reviewer: 4. Footgun Hunter: 9.

### Strong-signal items (≥2 lenses agree)

1. **passphrase_rotated audit lies on orphan rename** — BA2-F2 (High) + FG3-F3 (Critical). Audit row commits inside rewrap txn (BA-F4 atomicity) BEFORE the post-commit salt-file rename. If rename OSErrors, audit says "rotated" but on-disk salt is OLD → next boot KDF derives OLD KEK against NEW ciphertexts → unbootable system with no forensic signal. **Promoted to Critical.** Fix: 2-phase audit (`passphrase_rotation_started` in-txn + `passphrase_rotated` post-rename standalone).
2. **Retention loop infinite spin with no degraded-mode signal** — BA2-F3 (Medium) + FG3-F2 (Critical). Loop spins on persistent DB failure for process lifetime, ONE warning per 6h. Retention floor silently not enforced. **Promoted to Critical.** Fix: consecutive-failure counter + escalation to ERROR + `/readyz` flag.
3. **`_wipe_per_session_credentials` O(N) unbounded write-lock** — BA2-F5 (Medium) + FG3-F5 (High). No per-session-target ceiling; 30+ VK targets → OBS auth webhook blocked → publish drops. Pre-BA-F4 release window gone. **Confirmed High.** Fix: hard cap `MAX_PER_SESSION_TARGETS=10` validated at config or chunk merged txn.

### Single-lens findings (sorted by promoted severity)

**Critical (single-lens but verified):**
- **CR2-Re-F1** — `run_wal_checkpoint` outside try/except. Permanent loop kill on transient SQLITE_BUSY. Confidence 95. NEW bug introduced by slice 8. **Critical.**
- **FG3-F1** — Boot-loop on WAL contention at startup. `app_started` audit + `append_standalone(checkpoint=True)` can hit `OperationalError` after unclean shutdown → boot fails → s6 restarts → infinite loop with operator locked out. **Critical.**
- **FG3-F4** — Audit-of-audit row participates in retention DELETE. Long-horizon recursion compounds noise to 96.6% of table on low-activity deployments. **Critical** (signal-to-noise pathology, not OOM).

**High (single-lens):**
- **BA2-F1** — `reveal_credential` decrypts BEFORE audit append. If audit/commit fails, plaintext materialised in handler frame; retry produces under-counted reveal trail.
- **BA2-F4** — Supervisor `_audit` revives per-event `wal_checkpoint(TRUNCATE)`. Defeats the BA-F4 invariant on the supervisor's hot path (worker_failed_open flapping).
- **FG3-F6** — `target_deleted` row has `target_id=NULL` (FK is SET NULL but in-txn FK check fires immediately, so the column is omitted). `list_by_target` queries for deleted targets find NOTHING. Silent forensic gap.
- **FG3-F7** — `password_changed` audit failure leaks through timing-floor `finally`. Generic 500 with normal timing — INDISTINGUISHABLE from a successful change. User believes password changed; old password still works. UX + security-posture confusion.

**Medium / Low:**
- BA2-F6, BA2-F7, BA2-F8, CR2-Re-F2, CR2-Re-F3, FG3-F9, FG3-F10, CR2-Re-F4
- SA2-F4, SA2-F5, SA2-F6, SA2-F7, SA2-F8, SA2-F9, SA2-F10, SA2-F11 (all docs)

### Action plan

| Slice | Findings to close | Reason |
|---|---|---|
| **8.5 fix-up** (immediate) | CR2-Re-F1 (loop-killer), CR2-Re-F2 (paired), FG3-F1 (boot-loop), FG3-F4 (recursion noise), BA2-F4 (checkpoint revival), BA2-F1 (reveal decrypt-after-audit), STRONG #1 (passphrase orphan), STRONG #2 (retention degraded signal), FG3-F6 (target_deleted FK), FG3-F7 (password_changed UX) | All Critical + code-level High. Real correctness issues introduced or surfaced by slice 8. |
| **Slice 9** (ADR docs cleanup, originally) | All SA2-F* (11 items), BA2-F6 (tick observability log), BA2-F7 (env-only ADR drift documented), BA2-F8 (drop audit_id from busy-warning) | Docs/observability hygiene. |
| **Slice 10** (Medium cleanup, originally) | STRONG #3 (wipe O(N) — cap), FG3-F9 (last_idle_checkpoint persistence), FG3-F10 (IntegrityError on supervisor race), CR2-Re-F3 (test gap — close alongside slice 8.5), CR2-Re-F4 (type: ignore cleanup) | Lower-priority correctness + test hygiene. CR2-Re-F3 pulls into slice 8.5 alongside CR2-Re-F1 fix. |

### Conclusion

**Slice 8 declared "done" prematurely** — the in-slice reviewer pass (Rule №4) had a narrow checklist that missed open-ended issues. Re-audit Checkpoint #2 surfaced 4 NEW Criticals (3 from Footgun-hunter, 1 from Code-reviewer) plus 4 High code-level findings. **A slice 8.5 fix-up was required before declaring Phase 5 closed.** ~10 findings to close; one working session.

The checkpoint succeeded at its stated job: catch what the narrow in-slice review missed. Notably the Footgun-Hunter lens (Rule №6) was the dominant source of Critical findings (3 of 4) — vindicating Rule №6's mandate.

---

## Slice 8.5 — Re-audit-driven fix-up CLOSE-OUT (2026-05-19)

All Critical findings closed. All code-level High findings closed (BA2-F1 analyzed-not-fixed; rationale documented). Three STRONG-signal items (≥2 lenses agreed) ALL closed in this slice. Remaining items (SA2-F1 through SA2-F11 + BA2-F7 + BA2-F8 + BA2-F5/FG3-F5 STRONG #3 + FG3-F9 + FG3-F10 + CR2-Re-F4) are docs / Medium / Low — flowing into slice 9 (ADR docs cleanup) and slice 10 (Medium cleanup pass).

### Closed findings (slice 8.5)

**4 Critical:**
- FG3-F1 — `app_started` lifespan emit passes `checkpoint=False` (boot no longer races a dirty WAL).
- FG3-F2 (STRONG with BA2-F3) — Retention loop tracks consecutive-failure counter; `_logger.critical` escalation at threshold = 5 (~30h). Future slice routes flag to `/api/health`.
- FG3-F3 (STRONG with BA2-F2) — Passphrase rotation 2-phase audit: `passphrase_rotation_started` in-txn + `passphrase_rotated` post-rename standalone + `passphrase_rotation_orphaned` on rename failure. Audit-as-truth restored.
- FG3-F4 — `audit_log_retention_run` excluded from delete predicate via `AUDIT_LOG_RETENTION_EXCLUDED_EVENT_TYPES`. Marker rows are eternal forensic record.
- CR2-Re-F1 — Retention loop wraps each `run_wal_checkpoint` in try/except. Checkpoint failures no longer kill the loop permanently.

**4 High code-level:**
- BA2-F2 (STRONG) — closed alongside FG3-F3.
- BA2-F3 (STRONG) — closed alongside FG3-F2.
- BA2-F4 — Supervisor `_audit` passes `checkpoint=False`. Restores the BA-F4 invariant (no per-event WAL checkpoints on the supervisor's hot path).
- FG3-F6 — `list_by_target` matches both FK column AND `json_extract(data_json, '$.target_id')`. Deleted-target audit events now surface from target-scoped queries.
- FG3-F7 — `change_password` returns 503 + `PASSWORD_CHANGE_RETRY` + `Retry-After: 5` on persist failure. Catches `(SQLAlchemyError, OSError, TimeoutError)`.

**2 Medium tangential:**
- CR2-Re-F2 — `last_idle_checkpoint` updated only on successful checkpoint.
- CR2-Re-F3 — Retention loop body extracted into testable `_audit_log_retention_once(...)` helper + `_AuditRetentionTickState` dataclass. New tests pin all hardening invariants.
- BA2-F6 — Retention loop emits `audit_log_retention_tick` at INFO on every successful iteration with `rows_deleted`, `cutoff`, `retention_days`, `checkpoint_ran`. Operators get a heartbeat via `journalctl | grep audit_log_retention_tick`.

### Analyzed-but-not-fixed

- BA2-F1 (`reveal_credential` decrypt-before-audit reorder) — proposed reorder doesn't change forensic outcome under FastAPI's response model. Documented in the BA2-F1 entry above.

### Deferred (slice 10 — Medium cleanup pile)

- BA2-F5 / FG3-F5 STRONG (wipe per-session credentials O(N) write-lock) — realistic trigger (50+ VK targets) is unlikely in current operator base; cap or chunk in slice 10.
- BA2-F8 (drop audit_id from busy-warning fields) — Low; tiny code change held in slice 10 to keep slice 9 pure-docs (no Rule №4 reviewer needed for docs slice per phase table).
- FG3-F9 (last_idle_checkpoint persistence across restarts) — Medium.
- FG3-F10 (IntegrityError on supervisor _audit race) — Medium defensive.
- CR2-Re-F4 (`# type: ignore[arg-type|operator]` cleanup) — Low.

### Slice 9 CLOSED — ADR docs cleanup (2026-05-19)

All 11 SA2-F* items + BA2-F7 closed in slice 9. Plus all 10 open original SA-F* items (SA-F1, F2, F5, F6, F7, F8, F9, F11, F12, F13) from the 2026-05-18 baseline.

**Modified docs (9 files):**
- `docs/architecture/ADR-0007-persistence.md` — 6 sub-fixes (SA2-F2 engine-config rewrite, SA2-F3 system-maintenance bucket, SA2-F5 dual-contract revisit trigger, SA2-F6 audit-of-audit rationale + 2-phase audit pattern, SA2-F7 consequences bullets, SA2-F10 vocab polish, BA2-F7 ADR-drift acknowledgement).
- `docs/architecture/ADR-0005-auth-model.md` — SA2-F1 (audit-scope pointer) + SA-F8 (NEW §"Bootstrap log-stream routing" sub-section).
- `docs/architecture/ADR-0006-secret-encryption.md` — SA2-F11 (Sqlcipher rejection cross-refs ADR-0007 + names specific enabled events).
- `docs/architecture/ADR-0001-backend-stack.md` — SA-F13 (NEW §"Single-process invariants" with 7-row table).
- `docs/architecture/ADR-0004-docker-shape.md` — SA-F1 (§Volume layout rewritten with "What is NOT in this layout" sub-section) + SA-F13 (§"Open questions" cross-refs ADR-0001 invariant list).
- `docs/architecture/ADR-0003-rtmp-ingest-and-fanout.md` — SA-F5 (NEW code-reconciliation sub-section under §"Reconnect policy + circuit breaker").
- `docs/architecture/ADR-0010-headless-restart.md` — SA-F12 (§Consequences/Harder gains 6-surface env-leak bullet).
- `docs/architecture/ADR-0011-health-liveness.md` — SA-F2 (NEW "intentional non-implementation" sub-paragraph) + SA-F11 (§Consequences/Harder gains no-/metrics bullet).
- `docs/architecture/phase-6-design-memo.md` — SA2-F9 (§Q7 slice-8 amendment).
- `docs/architecture/phase-9-design-memo.md` — SA2-F8 (§M slice-8 amendment).
- `docs/architecture/README.md` — index extended; ADR-0001/0007/0010/0011 marked "amended 2026-05-19".

**NEW ADRs (3 files):**
- `docs/architecture/ADR-0012-mediamtx-dev-sidecar.md` — SA-F6.
- `docs/architecture/ADR-0013-audit-event-vocabulary.md` — SA-F7 + SA2-F4 catalogue.
- `docs/architecture/ADR-0014-spa-url-cap.md` — SA-F9.

**Gates at slice 9 close:**
- backend mypy/pytest **UNCHANGED** (slice 9 = pure docs).
- frontend **UNCHANGED**.
- No Rule №4 reviewer pass — slice 9 is the pure-docs exception per the phase table.

### New files in slice 8.5

(None — slice 8.5 is a fix-up on existing files.)

### Modified files in slice 8.5

**Production (5 files):**
- `app/repositories/audit_log.py` — `delete_older_than(session, *, cutoff, exclude_event_types=())` parameter added; `list_by_target` JSON1 fallback for deleted targets.
- `app/main.py` — retention loop body extracted into testable `_audit_log_retention_once(...)` helper + `_AuditRetentionTickState` dataclass; consecutive-failure counter + critical escalation + tick heartbeat log + checkpoint try/except guards + `AUDIT_LOG_RETENTION_FAILURE_THRESHOLD` + `AUDIT_LOG_RETENTION_EXCLUDED_EVENT_TYPES` constants; `app_started` emit passes `checkpoint=False`.
- `app/fanout/supervisor.py` — `_audit` helper passes `checkpoint=False`.
- `app/api/security_api.py` — passphrase 2-phase audit (started in-txn + rotated/orphaned standalone post-rename).
- `app/api/errors.py` — new `ErrorCode.PASSWORD_CHANGE_RETRY = "password_change_retry"`.
- `app/api/auth.py` — `change_password` catches `(SQLAlchemyError, OSError, TimeoutError)` → 503 + Retry-After + `PASSWORD_CHANGE_RETRY`.

**Tests (3 files):**
- `tests/db/test_audit_log_retention.py` — +5 tests (retention_excludes_marker, list_by_target_via_data_json, checkpoint_failure_does_not_kill_loop, recovery_resets_failure_counter, threshold_escalates_to_critical).
- `tests/api/test_phase9_endpoints.py` — +2 tests (rotate_passphrase_emits_two_phase_audit, rotate_passphrase_orphan_rename_emits_audit_and_raises).
- `tests/api/test_auth.py` — +1 test (change_password_persist_failure_returns_503_with_retry_after).
- `tests/api/test_errors.py` — `PHASE_6_CODES_FROZEN` extended.

### Rule №4 reviewer pass (slice 8.5)

Agent `aabfc4cd22603ce48` — 0 Critical / 2 Important / 3 test-coverage notes. **All Rule №5 burndown claims verified.**

In-slice fixes applied:
- **Important #1** — `change_password` exception catch widened from `SQLAlchemyError` to `(SQLAlchemyError, OSError, TimeoutError)`. The merged-txn BA-F4 invariant guarantees atomicity regardless; the widening only improves UX disambiguation.
- **Test-coverage note #5** — orphan-path test added (`test_rotate_passphrase_orphan_rename_emits_audit_and_raises`) using `monkeypatch.setattr(Path, "replace", ...)`. Verifies `started + orphaned` both land, `rotated` does NOT, under simulated rename failure.

Accepted (no in-slice fix):
- **Important #3** (`contextlib.suppress` around orphan-emit audit) — reviewer agreed this is correct best-effort idiom, not a Rule №1 violation.
- **Test-coverage note #4** (`capfd` structlog stream fragility) — accepted trade-off; alternative would mock `_logger.critical` directly which couples the test to logger internals.
- **Test-coverage note #6** (loop-vs-helper integration gap) — accepted; the helper is the testable unit, the loop is a thin sleep-and-delegate wrapper.

### Gates at slice 8.5 close

- backend mypy: **72/72** unchanged
- backend pytest: **783/783** (was 775 at slice-8 close → +8 new tests in slice 8.5)
- frontend typecheck/lint/**37 vitest**/build **232 KB** gzipped — unchanged (slice 8.5 backend-only)

### Burndown after slice 8.5

- **~75/100 findings closed (~75%)** — slice 8.5 closed 11 of the 32 re-audit findings; remaining 21 are docs (slice 9) + Medium cleanup (slice 10) + analyzed-not-fixed BA2-F1
- **16/16 Critical drained (100%)** — original-audit Criticals all closed; re-audit Critical-class (FG3-F1/F2/F3/F4 + CR2-Re-F1) all closed in 8.5
- **Phase 5 truly COMPLETE** — slice 8 (transactional refactor) + slice 8.5 (re-audit fix-up) together close out Phase 5

### What's next

Phase 6 — slices 9 (ADR docs cleanup) and 10 (Medium cleanup). No further re-audit checkpoints required (the workflow's two re-audit slots are Phase 2 tail + Phase 5 tail, both consumed).

## Transcript recovery table

| Agent | Task ID | Notes |
|---|---|---|
| Backend Architect | `a53267590f13a925e` | 8 findings (2 H / 5 M / 1 L) — full text in burndown above. |
| Software Architect | `a140e9ad14e9184b7` | 11 findings (3 H / 6 M / 2 L) — all docs/ADR drift. |
| Code-reviewer | `a56fed3fd0aff3a5d` | 4 findings (1 H / 2 M / 1 L) — full text in burndown above. |
| Footgun Hunter | `a59924181eee31cec` | 9 findings (4 C / 3 H / 2 M) — full text in burndown above. |
