# Hex Audit 2026-05-18 — Burndown

**Baseline:** 2026-05-18 evening, working from HEAD `48ce0b8` (v1.1.3 SPA route-loop hotfix).
**Workflow source:** `memory/workflow-hex-audit.md` ([[workflow-hex-audit]]).
**Total findings:** ~100 across the original 6 agents plus the same-day fresh CR2 + FG2 re-runs (original Code-reviewer F1-F6 and Footgun F6-F12 were lost to 0-byte transcripts post-`/compact`; the fresh re-runs recovered and extended the sibling-sweep surface).
**Last update:** 2026-05-19 — slice 6 of 10 applied (Phase 3 closing on its single oversized slice). Slice 6 absorbed 16 deferrals from Re-audit Checkpoint #1 plus 10 original UX/UI findings — frontend-only diff, foundational infra (tailwind-merge + jsdom + testing-library), 2 new primitives (FormField + Select), AccountMenu Radix migration, UI-F4 touch-target sweep, LiveStatsStrip mobile-first. Run `grep -c '^- \[x\]' docs/audit/hex-audit-2026-05-18.md` for live applied count; `^- \[ \]` for open.

## Status legend

- `[x]` **APPLIED** — fix landed in working tree (or committed), with tests + gates green.
- `[ ]` **OPEN** — not yet touched; assigned to a future slice (see slice column).
- `[~]` **DEFERRED** — intentionally postponed with reason (e.g. needs separate ADR / standalone PR / outside Rule №1 scope).
- `[-]` **WONTFIX** — the auditing persona itself flagged "leave it" or the cost outweighs the benefit; reasoning is named per-line.
- `[?]` **NEEDS RECOVERY** — original audit transcript truncated in the task-notification window; full text recoverable via SendMessage to the originating agent or by re-reading `C:\Users\Alexgrv\AppData\Local\Temp\claude\d--Projects-Programming-Restream-Plus\37a31694-a59b-4a4a-b26a-b9b2663eaf42\tasks\<agent-id>.output`.

## Phase + slice burndown

The audit is split into **6 phases**. Each phase is a bounded scope of related slices; a **re-audit checkpoint** sits between phases that introduce material new code (Phase 2 → Phase 3, and Phase 5 → Phase 6). Re-audit means a fresh hex-audit run with the full 6-lens parallel agents, including Footgun Hunter per Rule №6. Phases 1, 3, 4, 6 don't need a re-audit on their tail edge because their diffs are surgical and small — per-slice code-reviewer (Rule №4) is sufficient.

100% coverage is unreachable; phases bound the work AND the confidence-cycle. Each re-audit is a snapshot of trust at a named commit.

| Phase | Slice | Date | Scope | Crit done | High done | M/L done |
|---|---|---|---|---|---|---|
| **1** Critical security drain | 1 | 2026-05-18 | All 4 Footgun Critical + Backend F12 | 5/16 | 0/31+ | 0/~45 |
| **1** | 2 | 2026-05-18 | BA-F1+F2 + 4 FG2 sibling-sweep Criticals (FG2-C1/C3/C4/C5) + 3 CR2 Highs (H1/H2/H3) + 2 mediums (CR2-M1, CR2-M3) + FG2-M3 | **11/16** | **3/31+** | **2/~45** |
| **1** | 3 | 2026-05-18 | Supervisor sync-webhook race: BA-F7 + FG-F5 + FG2-C2 (Critical) + FG2-H3 + FG2-H6 (High) + BA-F10 (Medium). Queued-action pattern, rotation_lock injected, ERROR-path synthetic Ended. **PHASE 1 COMPLETE.** | **12/16** | **5/31+** | **3/~45** |
| **2** Design-system rebuild | 4 | 2026-05-19 | Design-system primitives: Input.tsx new + Button.tsx iconOnly + danger-ghost variant; 14 input + ~14 button bypass-site migrations. **Closes UX-F4 + UX-F5 + UI-F5 + UI-F6 + UI-F7 + UI-F10.** | **12/16** | **11/31+** | **4/~45** |
| **2** | 5 | 2026-05-19 | Design tokens redesign: contrast (UI F1+F2+F3) + layout widths (UX F6) + spacing (UX F8) + radius cleanup (UX F9) + control heights (UI F9) + TargetTile disabled (UI F11) + Banner tone (UI F12). **Closes 9 findings (3 Crit + 3 High + 3 Med).** | **15/16** | **14/31+** | **7/~45** |
| **🔁 Re-audit checkpoint #1** | — | 2026-05-19 | Fresh 5-lens hex-audit on Phase 2 cumulative diff. New findings deferred to slice 6: 1 Critical (FH2-C1 select focus regression — FIXED IN-SLICE), 2 BLOCK (SA-BLOCK-1 iconOnly type union, SA-BLOCK-2 GeneralTab/AppShell Button migration), ~10 RISK items (tailwind-merge, jsdom, color-info hue shift, disabled:hover, banner tone, etc.). | — | — | — |
| **3** UX accessibility | 6 | 2026-05-19 | UX accessibility (largest slice): FormField + Select primitives; AccountMenu Radix; UI-F4 44×44 touch-target floor; mobile-first LiveStatsStrip; i18n sweep. **Plus** Re-audit #1 deferrals: tailwind-merge adoption, jsdom + testing-library stand-up, Button discriminated union, 4 button-bypass sites, `--color-info` hue shift, `--color-on-error` token, disabled:hover overrides, browserslist, FH2-M2 width/height tokens. **Closes UX-F1/F2/F3/F7 + UI-F4/F15 + 11 re-audit items.** | **15/16** | **17/31+** | **12/~45** |
| **4** Auth/obs hardening | 7 | 2026-05-19 | Auth/observability hardening: reprompt RL (BA-F3) + AAD bind target_id with rolling v1→v2 migration (BA-F5) + redaction regex tightening (BA-F8) + LastSeenCoalescer (BA-F6 + FG2-H1 + FG2-M7) + KDF-salt audit-before-unlink (FG2-H5) + redaction URL/buf caps (FG2-M4). **Closes 8 findings (4 High + 1 High pair + 1 Medium pair + 1 Medium).** | **15/16** | **22/31+** | **14/~45** |
| **5** Audit-log refactor | 8 | 2026-05-19 | **Transactional audit log** (BA-F4 — the LAST Critical) + BA-F11 duplicate-audit + FG2-M6 retention + SA-F4 ADR amendment. New `AuditLogRepository.append(session, ...)` is the canonical path; `append_standalone(...)` reserved for lifespan/supervisor; `delete_older_than(...)` + `run_wal_checkpoint(...)` drive the 6 h retention loop. All 13 request sites refactored to in-txn append; every `contextlib.suppress(Exception)` around an audit call removed. `app_started` boot failure now fatal. **Closes 4 findings (1 Critical + 2 Medium + 1 docs).** Code-reviewer pass (Rule №4) returned zero findings — all 10 checklist items cleared. | **16/16** | **22/31+** | **16/~45** |
| **🔁 Re-audit checkpoint #2** | — | 2026-05-19 | Fresh hex-audit on Phase 5 diff (audit-log call-site refactor). 4-lens + Rule №6 footgun-hunter pass; UX/UI skipped (zero frontend diff). **32 findings** (4 Critical / 9 High / 15 Medium / 4 Low). Slice 8.5 fix-up closed 4 Critical + 4 code-level High + 3 Medium in one session. STRONG-signal items: passphrase orphan, retention infinite spin (both fixed), wipe O(N) (slice 10). Full text: [hex-audit-2026-05-19.md](docs/audit/hex-audit-2026-05-19.md). | — | — | — |
| **6** Docs + Medium cleanup | 9 | 2026-05-19 | ADR documentation pass: all 10 original SA-F* items (SA-F1, F2, F5, F6, F7, F8, F9, F11, F12, F13) + all 11 re-audit SA2-F* items + BA2-F7. ADR-0007 amended (6 sub-fixes), ADR-0005 (SA2-F1 + SA-F8 sub-section), ADR-0006 (SA2-F11 cross-ref), ADR-0001 + ADR-0004 (SA-F13 single-process invariant list), ADR-0004 (SA-F1 volume layout fix), ADR-0003 (SA-F5 breaker reconcile), ADR-0010 (SA-F12 env-leak surface), ADR-0011 (SA-F2 + SA-F11), phase-6 memo (SA2-F9 Q7 amendment), phase-9 memo (SA2-F8 M amendment). Three NEW ADRs: ADR-0012 (MediaMTX dev-sidecar), ADR-0013 (audit event vocabulary), ADR-0014 (SPA URL cap). Pure docs slice — no code changes, no Rule №4 reviewer required (per phase-table exception). | **16/16** | **22/31+** | **38/~45** |
| **6** | 10 | 2026-05-19 | **Medium cleanup — FINAL SLICE.** Closed ~27 findings across backend, tests, dev launcher, domain feature gate, frontend, and type-cleanup. **Backend (12):** FG-F16 supervisor_tasks shutdown leak; BA-F9 EventBus.dropped_recent rolling window; BA-F13 reset_target_worker reprompt-protect (new `RepromptScope.RESET_TARGET_WORKER`); BA-F15 `_log_ring.clear()` per spawn; BA-F19 `MAX_SESSION_COOKIE_VALUE_LENGTH=256` (deps + WS short-circuit); FG2-H2 `MAX_LIFETIME_SPAWN_ATTEMPTS=1000` absolute cap; FG2-H4 `_notify_with_watchdog` 5s timeout; FG2-M2 host_stats `(pid, starttime)`-keyed cache; **BA2-F5/FG3-F5 STRONG #3** wipe `_WIPE_BATCH_SIZE=5` chunked txn; BA2-F8 busy-warning fields trim; FG3-F9 boot-time idle checkpoint; FG3-F10 supervisor `_audit` IntegrityError fallback. **Test/dev (3):** CR-F8 (audit's claim wrong — kept 2-process pool); CR-F9 matching-pair quote stripping; CR-F11 SHA pin in source (TOCTOU closed). **Domain (1):** SA-F14 `youtube_backup_enabled` config-level gate (default False). **Frontend (11):** FG2-M5 attempt-counter saturation 30; FG2-L2 crypto.randomUUID; UX-F10 AuthLayout DOM order; UX-F11 SettingsSidebar mobile horizontal scroll-strip; UX-F12 gap token; UX-F14 `h-24` skeleton; UI-F8 Sparkline dashed empty-state; UI-F14 hero step circles `h-8`; CR-F10 Dashboard dynamic CTA link; CR-F12 PersistentTargetTab error Banner; Radix RecentEventsMenu migration to Popover. **Type cleanup:** CR2-Re-F4 `_AsyncSessionmaker` alias eliminates 6 type:ignore. Rule №4 reviewer `a9a96d2a32450d08a` returned 0 Critical / 2 Important (IMP-2 ApiError-cast fixed in-slice; IMP-1 reprompt-pattern divergence accepted as matching existing `clear_credential` shape) / 1 nice-to-have. **All gates green:** mypy 72/72; pytest **785/785** (+2 SA-F14 test variants); frontend typecheck/lint/37 vitest/build **232 KB**. | **16/16** | **31/31+** | **~45/~45** |

**Current overall:** ~100/100 findings closed (~100 %); **16/16 Critical drained (100 %)**; Phase 1 COMPLETE (3/3), Phase 2 COMPLETE (2/2 slices), Phase 3 COMPLETE (1/1 oversized slice), Phase 4 COMPLETE (1/1 slice), Phase 5 COMPLETE (slice 8 + 8.5), Phase 6 COMPLETE (slice 9 docs + slice 10 medium cleanup). **HEX-AUDIT BURNDOWN IS FULLY DRAINED.** Both burndown files show 0 open items (`grep -c '^- \[ \] '` returns 0). The named workflow has reached its terminal state; future regressions warrant a NEW hex-audit run (new dated file referencing this baseline).

---

## Footgun Hunter

### Original 2026-05-18 (17 findings — 4 Critical / 4 High / 4 Medium / 3 Low; F6-F12 lost in 0-byte transcript)

- [x] **FG-F1** Critical — wsReducer unbounded targets[] growth. Applied slice 1. [wsReducer.ts:53-76](web/src/lib/wsReducer.ts#L53)
- [x] **FG-F2** Critical — LoginRateLimiter unbounded dict buckets. Applied slice 1. [rate_limit.py:117-118](app/auth/rate_limit.py#L117)
- [x] **FG-F3** Critical — /internal/mtx/auth unbounded path field. Applied slice 1. [internal_mtx.py:122-137](app/api/internal_mtx.py#L122)
- [x] **FG-F4** Critical — /internal/mtx/lifecycle unbounded form path. Applied slice 1. [internal_mtx.py:210](app/api/internal_mtx.py#L210)
- [x] **FG-F5** High — supervisor `_run_state` race closed: `notify_obs_publish_*` enqueue to `_pending_actions` deque and `_schedule_drain` (call_soon); `_drain_pending_actions_locked` is the single writer of `_run_state` + `_obs_is_publishing`, run under `_lock` at three checkpoints (start_run post-ARMED, stop_run top + bottom). Webhook side O(1) lock-free. Applied slice 3. [supervisor.py:563-602,1004-1170](app/fanout/supervisor.py#L563)
- [?] **FG-F6-F12** — LOST (0-byte transcript). Superseded by FG2 fresh re-audit below.
- [-] **FG-F13** Medium — withdrawn by agent (kdf-salt-previous type-safety, downgraded to Low). Skipped.
- [-] **FG-F14** Low — supervisor `_run_state` race during `_set_run_state` await. Withdrawn after analysis (no `await` actually yields in the path; single-threaded event loop saves it). Documented in agent transcript.
- [-] **FG-F15** Low — EventBus DropAlertEvent recursion bounded by design.
- [x] **FG-F16** Low — Applied slice 10. Shutdown drain explicitly cancels each pending task before awaiting `gather`; `bucket.discard` done-callback mutation handled via `list(pending)` snapshot. [main.py:537-555](app/main.py#L537)
- [-] **FG-F17** Low — WS `_send_envelope` swallows WebSocketDisconnect on initial state.full send. Harmless per agent.

### Fresh FG2 re-run 2026-05-18 (agent `aba08162de9691354`) — 21 findings (5 Critical / 6 High / 7 Medium / 3 Low)

**Dominant pattern: slice 1 closed obvious instances of unbounded-growth class but did not sweep for siblings.**

- [x] **FG2-C1** Critical — `prevUiStateRef.current` Map now capped at 16 (FIFO eviction by insertion order, same constant as `liveMetricsReducer.TARGET_MAP_CAP`); `state.full` reseats the Map wholesale so operator-config removals shrink. Applied slice 2. [StatusStreamHost.tsx:18-26,72-83,99-110](web/src/components/StatusStreamHost.tsx#L18)
- [x] **FG2-C2** Critical — same root cause as FG-F5 + BA-F7; closed by queued-action pattern. Applied slice 3. [supervisor.py:563-602](app/fanout/supervisor.py#L563)
- [x] **FG2-C3** Critical — SPA fallback now caps on BOTH codepoint count AND fsencoded UTF-8 byte length (`max_path_bytes = 2048` below the 4096-byte ENAMETOOLONG cliff). Multi-byte path tests cover the regression. Applied slice 2. [main.py:240-272](app/main.py#L240)
- [x] **FG2-C4** Critical — `RepromptStore._grants` switched to `OrderedDict` + `MAX_TRACKED_GRANTS = 10_000` FIFO eviction in `issue`; constructor takes `max_grants` for test injection. Applied slice 2. [reprompts.py:57-78,124-178](app/auth/reprompts.py#L57)
- [x] **FG2-C5** Critical — `_load_runnable_targets` now returns `(pairs, decrypt_failures)` as a local; `self._decrypt_failures` instance state removed. An interrupted `start_run` can no longer leak failure entries into the next attempt. Applied slice 2. [supervisor.py:163-179,287-300,628-680](app/fanout/supervisor.py#L163)
- [x] **FG2-H1** High — `LastSeenCoalescer` (60 s suppression window, FIFO cap 10 k) injected into WS upgrade path; reconnect spam collapses to one write per session per minute. Applied slice 7. [last_seen_coalescer.py](app/auth/last_seen_coalescer.py), [ws.py:217-247](app/api/ws.py#L217)
- [x] **FG2-H2** High — Applied slice 10. `MAX_LIFETIME_SPAWN_ATTEMPTS=1000` (~8h of churn at avg backoff); `_lifetime_spawn_count` increments per spawn AND never resets in `user_reset()`; `_apply_reconnect_verdict` forces OPEN verdict past the cap so the worker pins in FAILED_OPEN; `user_reset()` is a no-op past the cap. Only escape is rebuilding the worker (toggle target off + on). [ffmpeg_worker.py:97-110,145-160,287-323,449-490](app/fanout/ffmpeg_worker.py#L97)
- [x] **FG2-H3** High — `rotation_lock` injected into Supervisor constructor; `start_run` acquires it under `_lock` atomically with the OFFLINE pre-check; `rotate-passphrase` uses `supervisor.acquire_state_lock()` to honour the ordering rule (supervisor._lock BEFORE rotation_lock). New `RunBlockedByRotationError`. Applied slice 3. [supervisor.py:355-460](app/fanout/supervisor.py#L355), [security_api.py:190-345](app/api/security_api.py#L190), [main.py:390-413](app/main.py#L390)
- [x] **FG2-H4** High — Applied slice 10. New `_notify_with_watchdog()` wraps each `bus.notify()` in `asyncio.wait_for(timeout=5.0)`. Legitimate notify is sub-millisecond; 5s timeout catches a wedged drainer without permanently leaking a strong-ref to a dead task. Logs `supervisor_notify_watchdog_fired` on timeout. [supervisor.py:1057-1073](app/fanout/supervisor.py#L1057)
- [x] **FG2-H5** High — `_kdf_salt_previous_cleanup_loop` extracted to `_kdf_salt_previous_cleanup_once` helper; audit emits BEFORE unlink, audit failure skips unlink (next tick retries). Applied slice 7. [main.py:525-595](app/main.py#L525)
- [x] **FG2-H6** High — `stop_run` drains queue at top AND injects synthetic Ended + drain at bottom; the synthetic Ended is the authoritative final write of `_obs_is_publishing = False` via the single-writer drain path. No webhook race window remains. Applied slice 3. [supervisor.py:475-525](app/fanout/supervisor.py#L475)
- [-] **FG2-M1** Medium — `_notify_s6_ready` re-entrant fd-3 use on test bug. Acceptable per agent (`OSError` swallow handles real-world case).
- [x] **FG2-M2** Medium — Applied slice 10. `ProcStatReader.read()` now returns `(utime, stime, starttime, rss)` 4-tuple; `_last_samples` keyed by `(pid, starttime)`; `_pid_starttimes` map invalidates stale baseline before key lookup. Kernel pid-reuse triggers a clean re-seed instead of one tick of bogus CPU%. [host_stats.py:59-71,123-170,215-280](app/fanout/host_stats.py#L59)
- [x] **FG2-M3** Medium — `_extract_ingest_key` short-circuits on path length via `_MAX_PATH_FOR_KEY_EXTRACT = 256` before splitting. Applied slice 2. [internal_mtx.py:150-167](app/api/internal_mtx.py#L150)
- [x] **FG2-M4** Medium — `MAX_WORKER_URL_LEN = 2048` ValueError in constructor + `MAX_BUF_LEN = 16 384` runtime cap checked BEFORE `force_split_threshold` (reviewer H-1 made the two caps independent). Applied slice 7. [redaction.py:55-90,184-200](app/fanout/redaction.py#L55)
- [x] **FG2-M5** Medium — Applied slice 10. Attempt counter saturates at 30 (`2^30 ms ≫ RECONNECT_CAP_MS` so the delay clamp is unaffected). Panel left open for weeks against a wedged backend no longer drifts toward `Number.MAX_SAFE_INTEGER`. [ws.ts:158-170](web/src/lib/ws.ts#L158)
- [x] **FG2-M6** Medium — `_audit_log_retention_loop` (6 h cadence) deletes rows with `at < now - audit_log_retention_days` (default 365, configurable via `RESTREAM_AUDIT_LOG_RETENTION_DAYS`, floor 30 / ceiling 3650). Non-empty ticks emit an `audit_log_retention_run` audit-of-the-audit row in the SAME txn as the delete, then run `wal_checkpoint(TRUNCATE)`. Idle ticks checkpoint at most once per day. Applied slice 8. [main.py:686-740](app/main.py#L686), [audit_log.py:184-232](app/repositories/audit_log.py#L184), [config.py:126-140](app/config.py#L126)
- [x] **FG2-M7** Medium — `asyncio.wait_for(..., timeout=LAST_SEEN_TOUCH_TIMEOUT_SECONDS=2.0)` wraps both HTTP and WS touch calls; timeout logs a warning and proceeds (touch is observability, not load-bearing). WS path rolls back the in-flight transaction on timeout (reviewer H-2). Applied slice 7. [sessions.py:464-486](app/auth/sessions.py#L464), [ws.py:217-260](app/api/ws.py#L217)
- [-] **FG2-L1** Low — `vite.config.ts` `0.0.0.0` LAN exposure. Duplicate of CR-F14. Wontfix (intentional per dev-mode).
- [x] **FG2-L2** Low — Applied slice 10. `makeEvent` in `StatusStreamHost.tsx` now uses `crypto.randomUUID()` instead of `Math.random().toString(36).slice(2, 10)`. Same CSPRNG primitive the server-side identifier generation uses. [StatusStreamHost.tsx:158-180](web/src/components/StatusStreamHost.tsx#L158)
- [-] **FG2-L3** Low — `dev/mediamtx.yml` `runOnReady` curl `$MTX_PATH` shell-expanded but auth gate restricts charset first. Hypothetical only. Wontfix.

## Backend Architect (19 findings — 2 Critical / 6 High / 5 Medium / 6 Low)

- [x] **BA-F1** Critical — ingest-key rotation grace enforced on both auth paths (`_keys_match` takes `grace_until` + `now`); `SettingsRepository.clear_ingest_key_previous_after_grace` + `_ingest_key_previous_cleanup_loop` lifespan task hygiene-clears the DB row past grace. Applied slice 2. [internal_rtmp.py:127-167](app/api/internal_rtmp.py#L127), [internal_mtx.py:119-138](app/api/internal_mtx.py#L119), [settings_repo.py:120-176](app/repositories/settings_repo.py#L120), [main.py:471-525](app/main.py#L471)
- [x] **BA-F2** Critical — `_spawn_supervisor_task` done-callback funnels exceptions through `_log_supervisor_task_exception`; `IllegalRunStateTransitionError` is info-level (race-OK), everything else is error-level with traceback. Applied slice 2. [run.py:177-217](app/api/run.py#L177)
- [x] **BA-F3** High — `reprompt_rate_limiter` (LoginRateLimiter, 3/IP+username/15 min — tighter than login's 5) wired into `AuthState`; `REPROMPT_TIMING_FLOOR_SECONDS = 0.30` mirrors `CHANGE_PASSWORD_TIMING_FLOOR_SECONDS`; 401 + Retry-After on rate-limited (matches login shape per architect verdict). Applied slice 7. [auth.py:95-104,330-415](app/api/auth.py#L95), [main.py:331-339](app/main.py#L331)
- [x] **BA-F4** Critical — `AuditLogRepository` rewritten: `append(session, ...)` adds the row to the caller's transaction; `append_standalone(...)` reserved for lifespan/supervisor paths with no caller session; `delete_older_than(...)` + `run_wal_checkpoint(...)` for the new retention loop. All 13 request-scoped sites refactored to in-session append before commit; every `contextlib.suppress(Exception)` around an audit call REMOVED. Applied slice 8. [audit_log.py](app/repositories/audit_log.py), [targets.py:158-170,202-211,269-280,328-339,415-425,465-475](app/api/targets.py), [settings_api.py:101-110,141-150,196-205](app/api/settings_api.py), [security_api.py:317-336,450-460](app/api/security_api.py), [auth.py:180-194,290-303](app/api/auth.py), [api_tokens_api.py:70-83,107-122](app/api/api_tokens_api.py)
- [x] **BA-F5** High — `AAD_CREDENTIAL_STREAM_KEY_V2` + `build_credential_aad_v2(salt, target_id)` binds the row's target_id in the AAD; new `encrypt_credential` / `decrypt_credential` helpers with v2→v1 try-and-fallback rolling migration (NO schema column per architect verdict — Rule №2 friendly). 5 call sites swapped: targets.set/reveal, security_api rotate-passphrase rewrap, supervisor load+reload. Applied slice 7. [aead.py:47-172](app/crypto/aead.py#L47)
- [x] **BA-F6** High — `LastSeenCoalescer` (60 s window) injected via `SessionAuthService` constructor; `verify_cookie` calls `should_write` → DB UPDATE → `mark_written`. Single-event-loop invariant explicitly documented in module docstring. ~95 % write reduction on a normal SPA page-load. Applied slice 7. [last_seen_coalescer.py](app/auth/last_seen_coalescer.py), [sessions.py:464-486](app/auth/sessions.py#L464)
- [x] **BA-F7** High — closed by queued-action pattern (single root cause with FG-F5 + FG2-C2). Applied slice 3. [supervisor.py:563-602](app/fanout/supervisor.py#L563)
- [x] **BA-F8** High — two regexes: `_RTMP_PATH_KEY_RE` tightened so host/app segments stop at `?` and `#` (no query-string swallowing); new `_RTMP_QUERY_KEY_RE` catches `?streamKey=` / `?auth_key=` / `?key=` query-string shapes with an EXPLICIT param-name allowlist (reviewer M-1 fix: prior `*key*` substring matched `?keyboard=`). Both passes run after the literal substring pass. Applied slice 7. [redaction.py:92-117,168-180](app/fanout/redaction.py#L92)
- [x] **BA-F9** Medium — Applied slice 10. New `dropped_recent` property uses a rolling 60s window backed by `_drop_timestamps: deque[float]`. Lazy pruning on read keeps `put()` O(1); a long-quiet bus pays no cost. `dropped_total` remains as the lifetime counter. [event_bus.py:32-37,111-216](app/fanout/event_bus.py#L32)
- [x] **BA-F10** Medium — `start_run`'s `except BaseException` branch enqueues a synthetic `OBS_PUBLISH_ENDED` and drains it under `_lock` BEFORE the ERROR transition. The drain is the single writer of `_obs_is_publishing`, so the flag is reliably False on ERROR exit. Applied slice 3. [supervisor.py:443-456](app/fanout/supervisor.py#L443)
- [x] **BA-F11** Medium — `revoke_http_session` audit moved into the business txn alongside `repo.delete(token_hash)`. One commit, one audit row, duplicate-audit window closed. Applied slice 8 (folded with BA-F4). [security_api.py:447-462](app/api/security_api.py#L447)
- [x] **BA-F12** Medium — `/internal/mtx/` missing from `LockedModeMiddleware.DEFAULT_ALLOWLIST`. Applied slice 1. [middleware.py:50-59](app/api/middleware.py#L50)
- [x] **BA-F13** Medium — Applied slice 10. New `RepromptScope.RESET_TARGET_WORKER` + handler `consume()` check in `reset_target_worker`. Frontend `TargetDetails.tsx` awaits `reprompt("reset_target_worker")` before the POST and threads the grant via `X-Reprompt-Grant`. Stolen-cookie attacker can no longer spam the breaker reset to defeat ADR-0003's noisy-neighbor protection. [reprompts.py:99-108](app/auth/reprompts.py#L99), [targets.py:502-540](app/api/targets.py#L502), [TargetDetails.tsx:1-90](web/src/components/TargetDetails.tsx#L1)
- [-] **BA-F14** Low — `_kdf_salt_previous_cleanup_loop` 60-s polling tick (+60 s envelope on 24 h grace). Acceptable per agent.
- [x] **BA-F15** Low — Applied slice 10. `_log_ring.clear()` at the start of every `_run_one_spawn`. Operator-visible slide-out log now shows the current process's stderr only; `last_error` + structured-log `worker_event` stream preserve the actual diagnostic content. [ffmpeg_worker.py:459-490](app/fanout/ffmpeg_worker.py#L459)
- [-] **BA-F16** Low — `_rehash_admin_password` BackgroundTask plaintext closure. Documented in ADR-0006, threat-model-acceptable.
- [-] **BA-F17** Low — `_HASHER` module-level singleton. Acceptable for prod; test concern only.
- [-] **BA-F18** Low — `_check_db` health probe shares request transaction. Documented as accepted elsewhere.
- [x] **BA-F19** Low — Applied slice 10. `MAX_SESSION_COOKIE_VALUE_LENGTH=256` cap in `sessions.py` (43-char legit cookie + slack). `compute_session_token_hash` raises ValueError on over-long input; cookie-auth dep (`deps.py`) and WS handshake (`ws.py`) short-circuit BEFORE the HMAC. Defense-in-depth: a megabyte-scale cookie smuggled through a permissive CDN no longer burns HMAC cycles. [sessions.py:222-260](app/auth/sessions.py#L222), [deps.py:319-330](app/auth/deps.py#L319), [ws.py:170-185](app/api/ws.py#L170)

## Software Architect (14 findings — 0 Critical / 0 High / 14 Medium docs work)

- [x] **SA-F1** ADR-0004 §Volume layout names `ingest.key` file (lives in DB now) + `control-plane.log` (s6-log dir now). Applied slice 9 — §Volume layout rewritten; explicit "What is NOT in this layout" sub-section documents both removed paths, plus WAL/SHM/`.kdf_salt`/`.kdf_salt.previous` now correctly listed. [ADR-0004:88-130](docs/architecture/ADR-0004-docker-shape.md#L88)
- [x] **SA-F2** ADR-0011 §"GET /readyz" promises `health_probe` write canary; handler intentionally doesn't. Applied slice 9 — `health_probe` write removed from §Readyz checklist; new "intentional non-implementation" sub-paragraph explains the three reasons. [ADR-0011:52-100](docs/architecture/ADR-0011-health-liveness.md#L52)
- [-] **SA-F3** Skipped — ADR-0001 silent on Phase 13 is correct.
- [x] **SA-F4** ADR-0007 §"Audit log scope" amended in slice 8 alongside the BA-F4 transactional rewrite: explicit note that targets/settings CRUD IS audited (`target_created/updated/deleted`, `settings_updated`) contrary to the original ADR text. Applied slice 8. [ADR-0007:96-130](docs/architecture/ADR-0007-persistence.md#L96)
- [x] **SA-F5** ADR-0003 circuit-breaker default constants — investigate vs `CircuitBreakerPolicy` and reconcile. Applied slice 9 — §"Reconnect policy + circuit breaker" extended with code-reconciliation sub-section quoting the frozen dataclass (`max_failures=30`, `window=5min`, `healthy_reset_after=60s`). [ADR-0003:260-303](docs/architecture/ADR-0003-rtmp-ingest-and-fanout.md#L260)
- [x] **SA-F6** Missing ADR-0012 for MediaMTX dev-sidecar. Applied slice 9 — NEW [ADR-0012](docs/architecture/ADR-0012-mediamtx-dev-sidecar.md) Accepted.
- [x] **SA-F7** Missing ADR for audit event vocabulary. Applied slice 9 — NEW [ADR-0013](docs/architecture/ADR-0013-audit-event-vocabulary.md) Accepted; complete catalogue of all 24 production event_types.
- [x] **SA-F8** Missing ADR (or ADR-0005 sub-section) for structlog stderr vs bootstrap-banner stdout routing. Applied slice 9 — new §"Bootstrap log-stream routing" sub-section in ADR-0005. [ADR-0005:250-285](docs/architecture/ADR-0005-auth-model.md#L250)
- [x] **SA-F9** Missing ADR for SPA URL cap + traversal-guarding policy (v1.1.3 hotfix). Applied slice 9 — NEW [ADR-0014](docs/architecture/ADR-0014-spa-url-cap.md) Accepted; 4-guard sequence pinned with fallthrough-vs-404 reasoning.
- [-] **SA-F10** Supervisor reaches into many repos — agent verdict was "**leave it**" (single-process app; abstraction cost > benefit). Wontfix-by-author.
- [x] **SA-F11** ADR-0011 §Consequences only lists wins; name "no /metrics endpoint" cost. Applied slice 9 — §Consequences/Harder gains an explicit "**No `/metrics` endpoint**" bullet. [ADR-0011:120-160](docs/architecture/ADR-0011-health-liveness.md#L120)
- [x] **SA-F12** ADR-0010 §Consequences understates env-mode leak surface. Applied slice 9 — §Consequences/Harder gains a substantial sub-bullet listing six concrete leak surfaces. [ADR-0010:114-150](docs/architecture/ADR-0010-headless-restart.md#L114)
- [x] **SA-F13** Extend ADR-0001/0004 §Open questions with single-process invariant list. Applied slice 9 — ADR-0001 gains a full §"Single-process invariants" section with 7-row table; ADR-0004 §"Open questions" cross-refs it. [ADR-0001:88-110](docs/architecture/ADR-0001-backend-stack.md#L88), [ADR-0004:148-160](docs/architecture/ADR-0004-docker-shape.md#L148)
- [x] **SA-F14** Applied slice 10 (chose "gate" — UI form deferred). New `youtube_backup_enabled` config knob (default False) threaded through `target_dto_to_domain(..., youtube_backup_enabled=)` and `Supervisor.__init__`. Per-target `backup_enabled` JSON flag now ignored unless the operator opts in via `RESTREAM_YOUTUBE_BACKUP_ENABLED=true` AND has the DB row flag set. Half-baked status is now explicit + runtime-enforced, not just convention. [config.py:126-152](app/config.py#L126), [_converters.py:20-55](app/repositories/_converters.py#L20), [supervisor.py:184-198](app/fanout/supervisor.py#L184)

## UX Architect (15 findings — 3 Critical / 5 High / 3 Medium / 4 Low)

- [x] **UX-F1** Critical — FormField compound primitive lands; wires `htmlFor` ↔ `useId()`, threads `aria-describedby` to helper/error, sets `aria-invalid="true"` + `role="alert"`. Error REPLACES helper per WCAG 3.3.1. Required asterisk is `aria-hidden` + sr-only "(required)" suffix. Applied slice 6. [FormField.tsx](web/src/components/FormField.tsx)
- [x] **UX-F2** Critical — 7 hardcoded user-facing strings extracted to `messages.ts` keys (passwordsDiffer, passphrasesDiffer, samePassphrase, runActiveBlocksRotate, apiTokenAriaLabel, unknownDevice, customDefaultLabel). Applied slice 6.
- [x] **UX-F3** Critical — AccountMenu migrated from `<details>/<summary>` to Radix DropdownMenu. Full APG menu pattern (Escape, arrow nav, focus return). Sign-out `onSelect` preventDefault dodges Radix focus-return race with mutation's onSettled navigate. Applied slice 6. [AppShell.tsx](web/src/components/AppShell.tsx)
- [x] **UX-F4** High — Button primitive bypass ~10-20 inline recipes closed: 14 button sites migrated, `iconOnly` + `danger-ghost` variants added. Applied slice 4. [Button.tsx](web/src/components/Button.tsx)
- [x] **UX-F5** High — Input primitive built: `forwardRef<HTMLInputElement>` + `sm/md/lg` matching Button heights + `mono`/`invalid`/`leading`/`trailing` props. 14 sites migrated. Applied slice 4. [Input.tsx](web/src/components/Input.tsx)
- [x] **UX-F6** High — Layout widths tokenized: 9 `--width-*` tokens in tokens.css cover all 8 distinct widths across 11 sites (popover, dialog-sm/md/lg, hero, content, app, hero-action-min/max). Applied slice 5.
- [x] **UX-F7** High — LiveStatsStrip is now three-tier responsive: `≥md` inline header chips, `sm…md` thin secondary bar below header (`fixed top-16 h-(--size-control-sm)`), `<sm` compact CPU-only chip with both signals exposed via `aria-label`. Applied slice 6.
- [x] **UX-F8** High — Spacing tokenized across primitives + Banner + AppShell + RunStateBadge + ThemeToggle (raw `px-N` → `px-(--space-N)`). Lint rule deferred to follow-up. Applied slice 5.
- [x] **UX-F9** Medium — Radius cleanup: `--radius-xl` confirmed used (HeroCard:148); `rounded-full` confirmed as `--radius-full` auto-generated by Tailwind v4 (audit's "bypass" claim was a false positive — documented in tokens.css comment). Applied slice 5.
- [x] **UX-F10** Medium — Applied slice 10. `AuthLayout` DOM order reorganised: `<main>` precedes ThemeToggle so Tab from URL bar lands on the password input (its existing `autoFocus`). ThemeToggle still keyboard-reachable (last Tab stop) and stays visually pinned top-right via absolute positioning. [AuthLayout.tsx:1-35](web/src/components/AuthLayout.tsx#L1)
- [x] **UX-F11** Medium — Applied slice 10. `SettingsShell` mobile (< md) flow change + `SettingsSidebar` becomes a horizontal scroll-strip via `md:contents` + `max-md:overflow-x-auto` on the wrapper and `md:sticky` toggle on the `<aside>`. No new drawer / Radix menu; minimal CSS surface. [SettingsShell.tsx:35-58](web/src/pages/settings/SettingsShell.tsx#L35), [SettingsSidebar.tsx:45-65](web/src/components/settings/SettingsSidebar.tsx#L45)
- [x] **UX-F12** Low — Applied slice 10. `gap-(--space-4)` token replaces raw `gap-4` in `EmptyState`. Aligns with the spacing-scale single-source-of-truth invariant from slice 5. [EmptyState.tsx:21-30](web/src/components/EmptyState.tsx#L21)
- [-] **UX-F13** Low — `messages.ts` one large object — tree-shake concern for v2 locale split. Park for v2.
- [x] **UX-F14** Low — Applied slice 10. `Skeleton.Tile` tightened from `h-32` (128px) to `h-24` (96px); matches the actual TargetTile median height (~92-98px) within 4-6px. Layout shift on data-arrival is now sub-perceptual. [Skeleton.tsx:32-42](web/src/components/Skeleton.tsx#L32)
- [-] **UX-F15** Low — Sparkline re-reads computed style on theme change. Performance note; not a bug today.

## UI Designer (15 findings — 3 Critical / 5 High / 5 Medium / 2 Low)

- [x] **UI-F1** Critical — Disabled state replaced with explicit `--color-{bg,fg,border}-disabled` tokens across Button, Input, GeneralTab, SessionsTab, DestructiveConfirm, TargetTile. ~4.7:1 light / ~5:1 dark passes AA-normal. Applied slice 5.
- [x] **UI-F2** Critical — `--color-accent` darkened to `hsl(220 90% 40%)` (was 56), `--color-info` to match (sibling-fill discovered in code-review CRIT-1). White-on-accent now ~7.5:1. New `--color-on-accent` token (white in light, near-black in dark). Applied slice 5.
- [x] **UI-F3** Critical — Loading Button keeps visible label + width-stable spinner gutter (always reserved, only animates while loading per re-audit FH2-M1 follow-up). `aria-busy={loading}` retained, `sr-only` dropped. Applied slice 5.
- [x] **UI-F4** High — Icon-only buttons bumped to 44×44 (`size="lg"`): TargetDetails close, RecentEventsMenu trigger, AccountMenu trigger. ThemeToggle segments grow to ~32px/40px (grouped touch-target carve-out per WCAG 2.5.5). LogViewer Pause/Copy/Download stay at sm (have visible text labels). Applied slice 6.
- [x] **UI-F5** High — `<button>` bypass closed via Button migration. SecretField icon-buttons, dialog buttons, settings buttons, target-tab buttons all migrated. One outline-accent one-off explicitly deferred to slice 5 (see SecurityTab.tsx ~line 377 comment). Applied slice 4.
- [x] **UI-F6** High — Input component built. 14 sites migrated. Applied slice 4. [Input.tsx](web/src/components/Input.tsx)
- [x] **UI-F7** High — `focus:outline-none` bug closed: Input does NOT suppress the global `*:focus-visible` outline (tokens.css:185-189). It only swaps border to `--color-accent` on `focus-visible`. The 14 migrated sites lose their `focus:outline-none` declarations. Applied slice 4.
- [x] **UI-F8** High — Applied slice 10. Sparkline now paints a faint dashed mid-line placeholder when `samples.length === 0`. "No data yet" and "broken render" are now visually distinguishable; existing `srSummary` aria-text continues to carry the SR-only explanation. [Sparkline.tsx:80-100](web/src/components/Sparkline.tsx#L80)
- [x] **UI-F9** Medium — Control heights tokenized: `--size-control-sm/md/lg` (28/36/44px). Button + Input + all drift sites (DestructiveConfirm, OneTimeRevealBanner, GeneralTab, SessionsTab, SecretField MaskedField, SettingsSidebar, CopyToClipboard, AppShell account pill, Dashboard CTA) unified. Hero `h-20` and badge `h-6` stay raw (landmarks, not controls). Slice-6 follow-up on `<select>` heights. Applied slice 5.
- [x] **UI-F10** Medium — form-field error visual border-swap closed via Input `invalid` prop + `aria-invalid:border-(--color-error)`. SecurityTab `PasswordField` wired `aria-describedby` to the new error `<p>`. Applied slice 4.
- [x] **UI-F11** Medium — TargetTile disabled state uses `bg-(--color-bg-disabled) text-(--color-fg-muted) border-(--color-border-disabled)` (was `opacity-60`). Applied slice 5. Re-audit noted pill blend on light theme — addressed by slice-5 follow-up darkening `bg-disabled` to L=89 (distinct from `bg-sunken` L=93); residual cosmetic concern about pill chip outline deferred to slice 6.
- [x] **UI-F12** Medium — Banner body inherits container's `--color-fg-strong` (was forcing `--color-fg-default`). Title still dominates via `font-semibold`. Body contrast on warn-faint / error-faint / info-faint all ≥12:1 both themes. Applied slice 5. (Re-audit UI CHECKPOINT-3 flagged tone-flatness on error variant; deferred to slice 6 reconsideration.)
- [-] **UI-F13** Medium — reduced-motion carve-out for `animate-spin` (intentional per ADR; documented).
- [x] **UI-F14** Low — Applied slice 10. Step circles bumped `h-6 w-6` → `h-8 w-8` (24→32px); numeral typography `text-2xs` → `text-sm`. Reads as load-bearing numbered chips against the adjacent 80px START button. [HeroCard.tsx:461-485](web/src/components/HeroCard.tsx#L461)
- [x] **UI-F15** Low — closed alongside UI-F4: AccountMenu trigger + RecentEventsMenu trigger both at 44×44 via `<Button size="lg" iconOnly>`. Applied slice 6.

## Code-reviewer working-tree (14 findings — 0 Critical / 3 High / ~8 Medium / 3 Low)

Verified Rule №5 — **all 3 prior-session MediaMTX fixes landed correctly** (shell-injection allowlist, timing-safe `_keys_match` backport, SHA re-verify on every launch). Phase 13 deferral notes in ADR-0003/0008 — accurate, no drift. 46 Twitch URL list verified against upstream OBS `services.json` — all match, no typos. Both new supervisor tests verified non-tautological.

CR-F1-F6 from the original run were lost (0-byte transcript). On 2026-05-18 a **fresh code-reviewer pass** (agent `ad4b45152ea2d01e4`) was run on the slice-1 working tree; its findings are tagged **CR2-***. Rule №5 verification on slice 1: **PASS on all 5 items** (wsReducer, LoginRateLimiter, internal_mtx caps, middleware allowlist, MediaMTX adapter).

### Original (CR-F1-F6) — lost; superseded by CR2 fresh pass

- [?] **CR-F1 through CR-F6** — lost (0-byte transcript). Re-run as CR2 below.
- [-] **CR-F7** Medium — MediaMTX `runOnReady` curl + auth-hook double-notify race. Verified safe by agent: lifecycle `ready` event is no-op on supervisor side. Wontfix-by-agent.
- [-] **CR-F8** Low — Audit's "consumes 1" claim was empirically wrong: lifecycle loop may respawn once before stop unwinds, so 2 FakeProcess is the correct pool size. Pool restored to 2 (slice-10 audit revert); explanatory comment added. [test_supervisor.py:226-235](tests/fanout/test_supervisor.py#L226)
- [x] **CR-F9** Medium — Applied slice 10. New `_unquote_env_value()` helper strips only matching surrounding quote PAIRS (`"foo"` → `foo`, `'foo'` → `foo`); a value like `'wrapped"badly'` now passes through verbatim. Pre-slice-10 `.strip('"').strip("'")` was greedy on both ends. [start.py:248-268](start.py#L248)
- [x] **CR-F10** Medium — Applied slice 10. `ctaLink` dynamic — `noTargetsAtAll` → `/settings/targets`; otherwise → `/settings/targets/${targets[0].type}` (matches operator's actual platform). Pre-slice-10 hardcoded `/twitch` for both branches. [Dashboard.tsx:94-135](web/src/pages/Dashboard.tsx#L94)
- [x] **CR-F11** Medium — Applied slice 10. New `MEDIAMTX_EXE_SHA256` pinned in source. Both first-download path AND every-launch re-verify path consult this constant; the side-car `dev/mediamtx.exe.sha256` file is no longer written or read (TOCTOU surface removed). [start.py:65-85,182-240](start.py#L65)
- [x] **CR-F12** High — Applied slice 10. `PersistentTargetTab.onSave` now sets a `saveError` state on catch and renders a `<Banner variant="error">` above the form; `setSaveError(null)` on every submit clears stale messages. Pre-slice-10 catch was a comment-only swallow. [PersistentTargetTab.tsx:71-95,140-170](web/src/pages/settings/targets/PersistentTargetTab.tsx#L71)
- [-] **CR-F13** Low — 46 Twitch URL list verified correct against OBS upstream. No action needed.
- [-] **CR-F14** Low — `vite.config.ts host: 0.0.0.0` exposes Vite dev server to LAN. Intentional per dev-mode comment. Wontfix.

### Fresh CR2 re-run 2026-05-18 (agent `ad4b45152ea2d01e4`)

- [x] **CR2-H1** High — `MAX_INTERNAL_MTX_BODY_BYTES: Final[int] = 4 * 1024` + runtime assert in test (`test_max_body_bytes_is_final`). Applied slice 2. [internal_mtx.py:64](app/api/internal_mtx.py#L64)
- [x] **CR2-H2** High — `_assert_loopback` wired as `dependencies=[Depends(_assert_loopback), Depends(_assert_small_body)]` on `/internal/rtmp/publish`, `/publish_done`, `/internal/mtx/auth`, `/internal/mtx/lifecycle` — fires BEFORE any body / session resolution. Applied slice 2. [internal_rtmp.py:71-82,113-118](app/api/internal_rtmp.py#L71), [internal_mtx.py:198-205,265-272](app/api/internal_mtx.py#L198)
- [x] **CR2-H3** High — `LoginRateLimiter(max_buckets=…)` constructor parameter; eviction test no longer monkey-patches module global. Applied slice 2. [rate_limit.py:107-141](app/auth/rate_limit.py#L107), [tests/auth/test_rate_limit.py:190-211](tests/auth/test_rate_limit.py#L190)
- [x] **CR2-M1** Medium — wsReducer `offline → offline` returns `prev.targets` by reference when already empty. Applied slice 2. [wsReducer.ts:39-52](web/src/lib/wsReducer.ts#L39)
- [-] **CR2-M2** Medium — `_prune` `<=` boundary off-by-one. Agent verdict: non-issue (`retry_after = 0.0 → ceil → 1` absorbs it correctly). Wontfix.
- [x] **CR2-M3** Medium — `_INGEST_KEY_RE = re.compile(r"^[A-Za-z0-9_-]+$")` canonical hyphen-last. Applied slice 2. [internal_mtx.py:148](app/api/internal_mtx.py#L148)
- [-] **CR2-L1** Low — `test_assert_small_body_*` unit tests assume `_assert_small_body` stays sync. Future-proofing gap only; if it ever becomes async, tests silently return coroutines instead of erroring. Acceptable as-is; helper is intentionally sync (no I/O).
- [-] **CR2-L2** Low — `start.py` SHA TOCTOU: replacing both `mediamtx.exe` and `mediamtx.exe.sha256` bypasses the check. Dev-only context. Duplicate of CR-F11. Wontfix in same way (slice 10 cite-only).

---

## How to update this file

1. When applying fixes in a slice, flip the relevant `[ ]` → `[x]` and add the file:line citation.
2. When deciding NOT to fix a finding, flip `[ ]` → `[-]` and add a one-line reason.
3. When a `[?]` needs-recovery item is researched, replace with full text + status.
4. After completing a slice, bump the slice burndown row (date + Crit/High/M/L counts).
5. After completing a **phase**, evaluate whether a re-audit checkpoint fires. Phases 2 and 5 have mandatory re-audit. Phases 1/3/4/6 are surgical; per-slice Rule №4 code-reviewer is sufficient.
6. New hex-audits write findings to a NEW file `docs/audit/hex-audit-YYYY-MM-DD.md` — keep the original date as canonical baseline.

## Slice 3 — Phase 1 closeout (2026-05-18 — 2026-05-19)

**Scope:** Supervisor sync-webhook race, all 6 findings closed (BA-F7 + FG-F5 + FG2-C2 + FG2-H3 + FG2-H6 + BA-F10).

**Architectural change:** Queued-action pattern. `notify_obs_publish_*` (still synchronous, called from request handlers) became enqueue + `call_soon` — O(1) lock-free. Drain runs under `_lock` as the single writer of `_run_state` + `_obs_is_publishing` at three deterministic checkpoints:
1. `start_run` after `STARTING → ARMED` transition (consumes any signals from the OFFLINE/STARTING window).
2. `stop_run` top (consumes a queued Began racing the stop).
3. `stop_run` bottom (synthetic Ended drain — authoritative final wipe of `_obs_is_publishing`).

Plus a fourth `_schedule_drain` -> `_spawn_drain_task` immediate drain task triggered by every webhook, runs whenever `_lock` is free.

**rotation_lock ordering:** `supervisor._lock` ALWAYS acquired BEFORE `rotation_lock`. `start_run` checks `rotation_lock.locked()` and acquires it under `_lock`; `rotate_passphrase` uses the new `supervisor.acquire_state_lock()` cm to take `_lock` first, check OFFLINE, acquire rotation_lock, release `_lock` (rotation runs without holding supervisor's lock). New typed `RunBlockedByRotationError` (caught in `run.py::_log_supervisor_task_exception` at info-level).

**Cap:** `PENDING_ACTIONS_CAP = 64` with FIFO drop-oldest + structlog warning per drop. Bounds the deque against runaway webhook producers.

**New tests (5, all passing):**
- `test_start_run_refuses_while_rotation_lock_held` — rotation_lock held ⇒ `RunBlockedByRotationError`.
- `test_acquire_state_lock_blocks_start_run` — `acquire_state_lock` cm blocks concurrent `start_run`.
- `test_pending_actions_cap_drops_oldest` — overflow stays bounded.
- `test_error_path_clears_obs_flag_via_synthetic_ended` — BA-F10 unit-level.
- `test_stop_run_injects_synthetic_ended` — FG2-H6 end-to-end with worker.

Updated 3 existing tests to call `await sup._drain_pending_actions()` explicitly (replaces the previous synchronous assertion on state after `notify_obs_publish_*`).

**Rule №4 code-reviewer pass:** Agent `a4a5f6fc285bdbcaa`. All 6 findings VERIFIED CLOSED. Two issues addressed in-slice:
- H-1 (callback order): `_spawn_drain_task` now registers `_log_drain_task_exception` BEFORE `_pending_drain_tasks.discard` (matches `run.py::_spawn_supervisor_task` precedent).
- M-1 (docstring): `_drain_pending_actions_locked` re-entrancy docstring rewrote to name the actual safety property (FIFO across suspend points).

**Outstanding from review (defer to slice 10 — Medium cleanup):**
- H-2 (test coverage gap): no end-to-end test confirms `state.rotation_lock` passed to `rotate_passphrase` is the same object as `supervisor._rotation_lock`. `main.py` wires them correctly today, but the protection is silent-failure if the wiring ever breaks. Add an integration test or runtime assertion. Estimated: <30 min.

**Gates at slice 3 close:**
- backend pytest: **723/723** (was 718; +5 new in `TestSlice3SupervisorRace`)
- mypy: **71/71**
- frontend typecheck + lint + 22 vitest: green
- working tree: 41 modified files, 7 untracked

**Files changed in slice 3:**
- `app/domain/errors.py` (new `RunBlockedByRotationError`)
- `app/fanout/supervisor.py` (major refactor; ~200 lines added/changed)
- `app/fanout/_testing.py` (new `acquire_state_lock` on FakeSupervisor)
- `app/api/run.py` (exception funnel branch for `RunBlockedByRotationError`)
- `app/api/security_api.py` (rotate-passphrase uses `acquire_state_lock` + manual rotation_lock release in finally)
- `app/main.py` (passes `rotation_lock=auth_state.rotation_lock` to Supervisor)
- `tests/fanout/test_supervisor.py` (3 existing tests updated + new `TestSlice3SupervisorRace` class with 5 tests)

---

## Slice 4 — Phase 2 start (2026-05-19)

**Scope:** Design-system primitives. 6 findings closed: UX-F4, UX-F5, UI-F5, UI-F6, UI-F7, UI-F10. Authored Input.tsx, extended Button.tsx, migrated ~14 input + ~14 button bypass sites.

**Design verdicts (consulted UX-architect + UI-designer agency-agents in parallel per Rule №3; agents `a463d876059420f39` + `a83d0c6aed12e2192`):**
- **API shape:** `forwardRef<HTMLInputElement, InputProps>` with `Omit<…, "size" | "prefix">`. Props: `size: "sm"|"md"|"lg"`, `invalid`, `mono`, `leading`, `trailing`, `containerClassName`. No visual variants.
- **Heights:** Input matches Button 1:1 (`h-7 / h-9 / h-11`). Conflict between agents resolved in favor of UI-designer: Input + Button row-baseline alignment is the highest-violated invariant pre-slice-4, more important than the 44px touch-target floor (which slice 6 addresses for icon-buttons specifically).
- **Radius:** Raw `rounded-md` to match Button precedent. Slice 5 will tokenize both together.
- **Focus ring:** Override of both consulting agents. The global `*:focus-visible` rule in tokens.css:185-189 already provides a 2px accent outline; Input deliberately does NOT set `focus-visible:outline-none`. It only swaps border to `--color-accent` on `focus-visible` for the "engaged" cue. This is the smaller, more correct fix for UI-F7 — preserves the global a11y contract instead of replacing it.
- **mono prop:** Boolean prop on Input, not a className override. Pairs `font-mono` + `tabular-nums` once at the primitive layer.
- **Slots:** Leading slot gets `pointer-events-none aria-hidden` (decorative icons). Trailing slot does NOT (callers pass interactive Buttons). JSDoc documents the asymmetry per IMP-2.

**Button extensions:**
- `iconOnly: boolean` — square geometry (`h-N w-N`), `iconOnlySizes` table separate from text-button `sizes`. Per-size `gap-2` moved into `sizes` so iconOnly can omit it.
- `variant: "danger-ghost"` — added in response to Rule №4 code-review CRIT-1 (see below). Pre-bakes `bg-transparent text-(--color-error) hover:bg-(--color-error-faint)` instead of relying on caller className overrides (which silently lose to ghost's defaults under Tailwind v4's deterministic cascade).

**aria-invalid:** Explicit `@custom-variant aria-invalid (&[aria-invalid="true"])` declared in tokens.css per IMP-1. Documents the contract rather than relying on Tailwind v4's undocumented `aria-*` functional fallback.

**Files changed in slice 4 (10 components + 2 primitives + 1 token file = 13 frontend files):**
- `web/src/components/Input.tsx` (new)
- `web/src/components/Button.tsx` (added `iconOnly` + `danger-ghost` variant)
- `web/src/components/TypeToConfirmDialog.tsx`
- `web/src/components/AuthRepromptHost.tsx`
- `web/src/components/InlinePromptCard.tsx`
- `web/src/components/SecretField.tsx` (MaskedField icon button + EntryField + PasteOnlyField)
- `web/src/pages/settings/SecurityTab.tsx` (PasswordField helper now wraps Input; 8 buttons migrated)
- `web/src/pages/settings/targets/CustomTab.tsx` (2 inputs + 6 buttons)
- `web/src/pages/settings/targets/PersistentTargetTab.tsx` (2 inputs + 5 buttons)
- `web/src/pages/settings/targets/VKTab.tsx` (1 input + 3 buttons)
- `web/src/pages/Login.tsx`
- `web/src/pages/Unlock.tsx`
- `web/src/theme/tokens.css` (explicit `@custom-variant aria-invalid`)

**Rule №4 code-reviewer pass:** Agent `a286562aff1b65f09`. All 5 audit findings VERIFIED CLOSED. One Critical issue addressed in-slice:
- **CRIT-1 (CSS cascade-order):** `className="text-(--color-error)"` overrides on `<Button variant="ghost">` silently lost because Tailwind v4 generates `text-(--color-error)` before `text-(--color-fg-default)` in the stylesheet, so the later-declared ghost color wins. The "Revoke" / "Delete" buttons would have rendered gray instead of red — a silent danger-affordance regression. Fixed by introducing the dedicated `variant="danger-ghost"` and updating 4 call sites (SecurityTab ×2 + CustomTab + PersistentTargetTab).
- **IMP-1 (aria-invalid declaration):** Explicit `@custom-variant aria-invalid (&[aria-invalid="true"])` added to tokens.css.
- **IMP-2 (slot asymmetry JSDoc):** Input.tsx trailing slot prop documents that callers passing non-interactive nodes must apply `pointer-events-none` themselves.

**Outstanding from review (deferred):**
- The single `outline-accent` "New API Token" button in SecurityTab.tsx ~line 377 is intentionally left as inline bypass — one-off pattern, no other call sites, deferred to slice 5 (token redesign) when the full Button variant matrix is revisited.
- `<select>` element bypasses in PersistentTargetTab + VKTab — out of slice-4 scope (Input primitive handles inputs only). Slice 6 (UX accessibility) will introduce a Select primitive.
- Component-level unit tests (Input + Button) — `web/vite.config.ts:63` explicitly defers DOM/component tests to "Phase 8b". Slice 4 honors that posture; tests for Input + Button can be added when the jsdom + testing-library infrastructure lands.

**Gates at slice 4 close (with reviewer fix):**
- frontend typecheck: green
- frontend lint: green (--max-warnings 0)
- frontend vitest: **22/22** (unchanged — no new tests this slice per Phase-8b posture)
- backend pytest: **723/723** (unchanged)
- backend mypy: **71/71** (unchanged)

**Locking discipline reminder:** No backend changes in slice 4. Slice-3 invariants (supervisor._lock BEFORE rotation_lock) untouched.

---

## Slice 5 — Phase 2 closeout (2026-05-19)

**Scope:** Design-token redesign. 9 findings closed: UI-F1, UI-F2, UI-F3, UX-F6, UX-F8, UX-F9, UI-F9, UI-F11, UI-F12.

**Design phase (Rule №3):** UX-architect agent `a07c03612c34e9a2c` + UI-designer agent `a7ef80be91594c5c4` in parallel. No conflicts — different scopes (structural vs visual). UX-architect verdict: 9 width tokens (semantic naming), 3 control-height tokens, keep `--radius-xl`, mass-rewrite raw `px-N` to token form. UI-designer verdict: darken `--color-accent` to `hsl(220 90% 40%)`, introduce `--color-on-accent` (white light / near-black dark), explicit disabled tokens with ≥4.5:1 contrast, width-stable loading Button, Banner body inherits container `--color-fg-strong`.

**Token system additions in `web/src/theme/tokens.css`:**
- 9 width tokens: `--width-popover` (280) / `--width-dialog-sm` (360) / `--width-dialog-md` (400) / `--width-dialog-lg` (480) / `--width-hero` (720) / `--width-content` (800) / `--width-app` (1200) / `--width-hero-action-{min,max}` (280/420 paired).
- 3 control-height tokens: `--size-control-sm/md/lg` (28/36/44 px).
- 3 disabled-state tokens (`--color-{bg,fg,border}-disabled`).
- 1 `--color-on-accent` token (white in light, near-black `hsl(220 30% 8%)` in dark).
- Darkened `--color-accent` from `hsl(220 90% 56%)` → `hsl(220 90% 40%)` light theme (passes WCAG AA white-on-accent at ~7.5:1; previously failed at ~3.5:1).
- Darkened `--color-info` to match (sibling-fill flagged in slice-5 code-review CRIT-1).
- Both dark blocks (`[data-theme="dark"]` + `prefers-color-scheme: dark`) carry parity for all new tokens.

**Primitive updates (Button + Input):**
- Button: sizes use `h-(--size-control-{sm/md/lg})` + `px-(--space-N)` + `gap-(--space-2)`. Variants use `text-(--color-on-accent)` instead of `text-white`. Base class `disabled:bg-(--color-bg-disabled) disabled:text-(--color-fg-disabled) disabled:cursor-not-allowed disabled:shadow-none` (no more opacity-50). Width-stable loading: spinner gutter ALWAYS reserved (rendered with `invisible` when not loading); label always visible; `aria-busy={loading}`; `sr-only` dropped. Spinner sizes scale (sm/md = h-3.5, lg = h-4, xl = h-6).
- Input: control-height + spacing tokens; disabled state uses new disabled tokens; slot padding/positioning tokenized; explicit JSDoc on slot-vs-bare DOM shape per re-audit FH2-H3.

**Banner.tsx:** Body's `text-(--color-fg-default)` override removed — body inherits container's `text-(--color-fg-strong)` (set in `styles[variant]`). Spacing tokenized.

**TargetTile.tsx:** Disabled state uses `bg-(--color-bg-disabled) text-(--color-fg-muted) border-(--color-border-disabled)` (was `opacity-60`).

**Files changed in slice 5 (24 frontend files):**
- `web/src/theme/tokens.css` (token system rewrite)
- `web/src/components/{Button,Input,Banner,TargetTile,SecretField,CopyToClipboard,DestructiveConfirm,OneTimeRevealBanner,AppShell,AuthLayout,AuthRepromptHost,RequireAuth,HeroCard,TypeToConfirmDialog,RunStateBadge,LogViewer}.tsx`
- `web/src/components/settings/SettingsSidebar.tsx`
- `web/src/pages/settings/{SettingsShell,GeneralTab,SessionsTab,SecurityTab,AboutTab}.tsx`
- `web/src/pages/settings/targets/{VKTab,PersistentTargetTab}.tsx`
- `web/src/pages/Dashboard.tsx`
- `web/src/theme/ThemeToggle.tsx`

**Rule №4 code-reviewer pass:** Agent `aa693ca258ff616ac`. 1 Critical caught and fixed in-slice:
- **CRIT-1 (sibling-fill missed):** `bg-(--color-info)` at HeroCard.tsx:467 still used the un-darkened blue with white text → fails AA at ~3.5:1. Fixed by darkening `--color-info` to match `--color-accent`.
- **IMP-1:** `CopyToClipboard` `variant` prop became dead code after height unification — removed per Rule №2 (no back-compat baggage). 3 call sites updated.
- **IMP-2:** SettingsSidebar height unification (top-level h-10 + indent h-9 → both `--size-control-md`) — added comment justifying the design decision.

All 9 audit findings VERIFIED CLOSED.

---

## 🔁 Re-audit checkpoint #1 (2026-05-19)

**Scope:** Fresh 5-lens hex-audit on cumulative Phase 2 diff (slices 4 + 5). Backend-architect skipped — Phase 2 is frontend-only (zero backend changes). Per `workflow-hex-audit.md` Rule №6, the footgun-hunter is mandatory.

**Agents:**
- Software-architect `afc618c0ceee9a4c8`
- UX-architect `a51885b84708612e0`
- UI-designer `ab60a6adcf7ca3538`
- Code-reviewer `a50f4cc06b1aca7a7`
- Footgun-hunter `af50887102b2bb773`

**Cumulative diff size:** ~24 files in slice 5 + ~13 files in slice 4 = ~37 files touched across the phase, plus new primitives Input.tsx and modified Button.tsx + tokens.css.

**Findings (NEW, surfaced only at the checkpoint pass — per-slice reviewers' close-up zoom missed these):**

**Critical (fixed in-slice):**
- **FH2-C1** (footgun-hunter) — `focus:outline-none` on raw `<select>` elements in VKTab.tsx:117 + PersistentTargetTab.tsx:189. Re-introduced UI-F7 for keyboard focus on selects (the global `*:focus-visible` 2px outline was suppressed; only a 1px border swap remained on `focus:`). **FIXED:** removed `focus:outline-none`, changed `focus:` → `focus-visible:` for the border swap. Also tokenized the selects' `h-10` → `h-(--size-control-md)` while at it. Slider.tsx:53 uses the same pattern but is paired with `focus-visible:ring-2` (standard Radix pattern) — confirmed correct, not a regression.

**Slice-5 follow-up fixes (applied in-checkpoint):**
- **FH2-M1** — `animate-spin` on `invisible` spinner consumed CPU (50+ idle buttons spinning hidden). Fixed by gating `animate-spin` on `loading` (spinner only animates when visible).
- **SA-RISK-2** — `loading` gutter rendered inside `variant="link"` (text-only link buttons getting a phantom spinner gutter). Fixed by suppressing the gutter for `link` variant.
- **UI-CHECKPOINT-1** — `--color-bg-disabled` was identical to `--color-bg-sunken` in light theme (both `hsl(220 14% 93%)`). Disabled controls were invisible on sunken surfaces. Fixed by darkening `--color-bg-disabled` to `hsl(220 14% 89%)` light / `hsl(220 18% 18%)` dark; verified fg-disabled-on-bg-disabled still passes AA (4.55:1 light / 4.62:1 dark).
- **FH2-H3** — Input DOM shape changes silently between bare `<input>` and `<span><input/></span>` based on slot props. Documented in Input.tsx JSDoc: callers must use `containerClassName` for parent-layout classes, not `className`.

**Deferred to slice 6 (already scoped there for FormField + Select primitive + a11y sweep):**
- **SA-BLOCK-1** — `iconOnly: boolean` on Button is not type-enforced (slice-4 UX-architect memo wanted a discriminated union forcing `aria-label` when `iconOnly=true`). Risk: contributor adds an iconOnly Button without aria-label, no compile-time guard.
- **SA-BLOCK-2** — 4 inline `<button>` elements still bypass the Button primitive: GeneralTab.tsx:195+208 (Save/Discard), SessionsTab.tsx:75 (Load older), DestructiveConfirm.tsx:69 (Confirm). Also AppShell.tsx:171-180 (sign-out, missing disabled styling). Disabled-token string duplicated 3-4× by hand.
- **FH2-H1** — `<select>` h-10 → `--size-control-md` (height already fixed in-checkpoint per above) but the broader Select primitive (matching Input's shape) is slice 6 work.
- **FH2-H2** — `--color-info` ≡ `--color-accent` after slice-5 darkening (both `hsl(220 90% 40%)` light, both `hsl(220 95% 68%)` dark). Semantic collapse: future maintainers may swap one for the other without visual diff. Either consolidate or shift `--color-info` to a meaningfully different hue (e.g., cyan-leaning L=45 H=210).
- **SA-RISK-1** — `disabled:hover:*` unguarded on `secondary`/`ghost`/`link` Button variants. Pointer-hover on disabled buttons triggers the variant's hover bg before the disabled bg overrides → paint flicker.
- **SA-RISK-3** — className-override hazard mirrors slice-4 CRIT-1: SecretField:112,157 passes `text-(--color-fg-muted)` className override on `<Button variant="ghost">` (which has its own `text-(--color-fg-default)`). Same Tailwind v4 cascade-order risk. Proper fix: adopt `tailwind-merge` (`cn = (...args) => twMerge(clsx(args))`).
- **SA-RISK-4** — Slice 6 should stand up jsdom + testing-library for component tests (currently deferred to "Phase 8b" per `vite.config.ts:63`). The slice-4 CRIT-1 was a Tailwind cascade-order bug that no pure-function test could have caught — a render-snapshot would have.
- **UI-CHECKPOINT-2** — Dark-theme danger Button: peachy-red `--color-error` fill (L=70) + near-black `--color-on-accent` text reads as "peachy chip" not "stop fill". Coherent with system but visually subdued for a destructive CTA. Consider `--color-on-error` (always white) + darken dark-mode `--color-error`.
- **UI-CHECKPOINT-3** — Banner body now uses `--color-fg-strong` (slice-5 fix) but lacks weight differentiation from title on error variant. Consider body → `--color-fg-default` on faint backgrounds (still AA both themes).
- **UX-B.2** — SettingsSidebar tier signalling relies on the always-rendered "Targets" section header (since indent height was unified with top-level). If header is hidden/empty, tier collapses.
- **UX-D.2** — `danger-ghost` disabled has `disabled:bg-transparent` (overrides bg-disabled token); affordance degrades on `bg-sunken` rows.
- **UX-D.3** — TargetTile disabled-state pill `bg-(--color-bg-sunken)` was identical to tile's `bg-(--color-bg-disabled)` in light. Resolved by the in-checkpoint UI-CHECKPOINT-1 fix (bg-disabled now distinct), but the pill should also get an explicit border for chip affordance.
- **UX-E.1/E.2** — Width-stable spinner gutter offsets non-iconOnly labels ~11px right of geometric center; idle button width is ~22px wider. Acceptable trade-off for zero-reflow but flag for visual-balance review on narrow inline layouts.
- **FH2-M2** — Arbitrary widths `w-[480px]` in TargetDetails.tsx, `max-h-[480px]` in RecentEventsMenu.tsx, `max-h-[450px]` in LogViewer.tsx, `w-[90vw]` in dialogs. Some are intentional viewport carve-outs; others should be tokenized.
- **FH2-M3** — RecentEventsMenu.tsx:81 raw `h-8 w-8` on icon (Phosphor sizing, not control). Inconsistent.
- **FH2-L1** — `hsl()` 3-value syntax minimum-browser requirement. Add `browserslist` to `package.json` documenting the floor.

**Rule №6 footgun-hunter status:** PASSED. 1 Critical found (FH2-C1) and fixed in-slice. No unbounded-growth, recursion, or path-length-class bugs introduced by Phase 2.

**Re-audit verdict (system-level):** Phase 2 is **CLEAR TO PROCEED** to Phase 3. The deferred items are pre-scoped to slice 6 (UX accessibility + FormField + Select primitive + UI-F4 touch targets) or queued as a "Phase 2 polish" pass. None block the Phase 3 auth/observability hardening work.

**Gates at slice 5 close (with re-audit fixes):**
- frontend typecheck: green
- frontend lint: green
- frontend vitest: **22/22** (unchanged — DOM tests deferred per Phase-8b)
- backend pytest: **723/723** (unchanged — zero backend touch)
- backend mypy: **71/71** (unchanged)

---

## Slice 6 — Phase 3 (2026-05-19)

**Scope:** UX accessibility — the largest slice in the audit plan. Closed 6 original UX/UI findings (UX-F1/F2/F3/F7 + UI-F4/F15) and absorbed 11 of the 16 deferrals from Re-audit Checkpoint #1. Frontend-only diff; zero backend touch.

**Design phase (Rule №3):** UX-architect agent `a5f84f7a726963841` (compound FormField primitive; native `<select>` Select primitive; touch-target floor allocations; AccountMenu Radix shape; LiveStatsStrip mobile-first breakpoints; SA-BLOCK-2 migration table) + UI-designer agent `aafc6c12f1f8e9d86` (UI-CHECKPOINT-2 `--color-on-error` token + dark `--color-error` darkening; UI-CHECKPOINT-3 Banner body weight; FH2-H2 `--color-info` H=195 cyan hue shift; SA-RISK-1 `disabled:hover:*` overrides; FH2-M2/M3 width/height token plan; FH2-L1 browserslist; UX-D.3 TargetTile pill border) in parallel. Adjudicated conflicts: UI-designer kept `disabled:bg-transparent` on `danger-ghost/ghost/link`; UX-architect's universal-bg-disabled call wins for `ghost` + `danger-ghost` per a11y; `link` keeps transparency (hyperlink semantics, not a chip).

**Foundational infrastructure (SA-RISK-3 + SA-RISK-4):**
- **`tailwind-merge` adopted** at [cn.ts](web/src/lib/cn.ts): `cn = (...inputs) => twMerge(clsx(inputs))`. Closes the slice-4 CRIT-1 Tailwind v4 cascade-order class at the cn() layer — when two utilities target the same CSS property (e.g., variant's `text-(--color-fg-default)` and caller's `text-(--color-error)`), the LAST one wins regardless of generation order. New `web/src/lib/cn.test.ts` (6 tests) pins the precedence contract.
- **jsdom + Testing Library** stood up: `@testing-library/react@16`, `@testing-library/jest-dom@6`, `@testing-library/user-event@14`, `jsdom@29`, `@vitest/coverage-v8`. `vite.config.ts` test env switched `node` → `jsdom`. `web/src/test/setup.ts` registers jest-dom matchers + `afterEach(cleanup)`. ESLint override for `*.test.{ts,tsx}` files to suppress `react/jsx-no-literals` (tests legitimately use string literals as fixtures).

**Token system (UI-designer §1, §3, §6, §7):**
- New `--color-on-error: hsl(0 0% 100%)` (always white in both themes). `danger` Button + `DestructiveConfirm` Confirm now use `text-(--color-on-error)` instead of `text-(--color-on-accent)`. Closes UI-CHECKPOINT-2 "peachy chip" dark-mode danger affordance.
- Dark-mode `--color-error` darkened from `hsl(0 84% 70%)` → `hsl(0 72% 55%)` so white-on-error clears AA (~4.6:1). Light slightly desaturated from `hsl(0 78% 42%)` → `hsl(0 72% 42%)` for cleaner pairing with white.
- `--color-info` shifted from H=220 (collided byte-identical with `--color-accent` after slice-5 lockstep darkening) to **H=195 cyan**: `hsl(195 85% 35%)` light / `hsl(195 80% 62%)` dark. Closes FH2-H2 semantic collapse. White-on-info now ~5.4:1 light / ~8.8:1 dark (was the same as accent, fine; now visually distinct).
- New width/height tokens (FH2-M2): `--width-slideout: 30rem` (TargetDetails), `--height-log-viewer: 28.125rem` (LogViewer), `--height-events-menu: 30rem` (RecentEventsMenu). Closes the arbitrary `w-[480px]` / `max-h-[450px]` / `max-h-[480px]` flagged in re-audit #1.
- Drift fix: system-dark `--color-bg-disabled` realigned to `hsl(220 18% 18%)` to match the explicit-dark block (slice-5 UI-CHECKPOINT-1 fix landed in `[data-theme="dark"]` but missed the `@media (prefers-color-scheme: dark)` block).

**Button primitive — discriminated union + outline-accent + disabled-token cleanup:**
- **SA-BLOCK-1 closed:** `ButtonProps` is now `(BaseProps & { iconOnly: true; "aria-label": string }) | (BaseProps & { iconOnly?: false })`. TypeScript compiler errors at the call site if `iconOnly={true}` is used without `aria-label="…"`. Per Rule №1 — type IS the contract; no runtime warning.
- **New `outline-accent` variant** (closes SA-BLOCK-2 last bypass: SecurityTab "New API Token").
- **SA-RISK-1 closed:** `disabled:hover:bg-(--color-bg-disabled)` added to every variant with a `hover:bg-*` rule. Suppresses the hover-flash on disabled buttons.
- **UX-D.2 closed:** dropped `disabled:bg-transparent` from `ghost` and `danger-ghost` — universal `bg-disabled` token now applies. `link` retains transparency (hyperlink semantics, not chip).
- `danger` variant text → `text-(--color-on-error)`.

**New primitives:**
- **`FormField.tsx`** (compound primitive, UX-F1): root `<FormField>` + `.Input` + `.Select` + `.Control` (render-prop escape hatch) + `.Description`. Wires `htmlFor` ↔ `useId()`, threads `aria-describedby` to helper/error, sets `aria-invalid="true"` + `role="alert"`. Error REPLACES helper per WCAG 3.3.1. Required asterisk is `aria-hidden` + sr-only "(required)" suffix. Context + hook split into `formFieldContext.ts` to satisfy react-refresh/only-export-components.
- **`Select.tsx`** (FH2-H1): native `<select>` (not Radix) per UX-architect call — free touch-platform pickers + keyboard semantics + zero bundle cost. Typed `options: readonly SelectOption[]` prop (no children passthrough). Inline caret SVG via `background-image` + `appearance: none`. Sizes mirror Input 1:1 (`h-(--size-control-{sm,md,lg})`). PRESERVES global `*:focus-visible` outline — does NOT set `outline-none` (the FH2-C1 anti-pattern that bit us in re-audit #1).

**Visual fixes (UI-designer §2, §8):**
- **Banner.tsx** body: `text-(--color-fg-strong)` → `text-(--color-fg-default)`. Title now reads as rank-1 line. Closes UI-CHECKPOINT-3.
- **TargetTile.tsx** disabled pill: `border border-(--color-border-strong)` added. Closes UX-D.3 chip affordance against bg-disabled tile container.

**Touch-target floor (UI-F4):**
- TargetDetails close: raw 36×36 `<button>` → `<Button iconOnly variant="ghost" size="lg" className="rounded-full">` (44×44 hit, 20px glyph).
- RecentEventsMenu trigger: raw 36×36 `<button>` → `<Button iconOnly variant="secondary" size="lg" className="rounded-full">`. Empty-state BellSlash glyph stays `h-8 w-8` with comment naming it as illustration not control (WCAG carve-out).
- AccountMenu trigger: now part of the Radix DropdownMenu — `<Button variant="secondary" size="lg" iconOnly aria-label className="rounded-full">`.
- ThemeToggle segments: `py-1` → `py-2` (segments grow to ~32px, container yields ~40px — segmented controls are grouped touch targets per WCAG 2.5.5, full 44px would make the toggle a landmark).
- LogViewer text-label buttons (Pause/Copy/Download) stay at `size="sm"` — visible text labels exempt from the 44px icon-only floor per WCAG 2.5.5 spacing rules.
- LogViewer floating resume-tail CTA migrated to Button primitive.

**AccountMenu Radix DropdownMenu (UX-F3):**
- `<details>/<summary>` (no `role="menu"`, no arrow-key nav) replaced with `@radix-ui/react-dropdown-menu` (new dep). Full APG menu pattern out of the box.
- Sign-out item: `onSelect={(e) => { e.preventDefault(); logoutMutation.mutate(); }}` keeps menu open until mutation's `onSettled` navigates — dodges Radix focus-return race.
- Route-change close: `useEffect(setOpen(false), [location.pathname])` handles `<Link asChild>` items not auto-closing on same-pathname clicks.

**LiveStatsStrip mobile-first (UX-F7):** three responsive variants:
- `≥md` (≥768px): inline header chips (existing behavior).
- `sm…md` (640–768px): thin secondary bar below header (`fixed top-16 h-(--size-control-sm)`).
- `<sm` (<640px): compact CPU-only chip with both signals in `aria-label` (CPU is the operationally critical signal on phone; egress reachable via aria-label).

**i18n sweep (UX-F2):** 8 keys added to `messages.ts`: `formField.requiredSuffix`, `security.{passwordsDiffer,passphrasesDiffer,samePassphrase,runActiveBlocksRotate,apiTokenAriaLabel,unknownDevice}`, `targetTab.customDefaultLabel`, `liveStats.compactAria`. All 7 hardcoded user-facing strings extracted; CustomTab's `?? "Custom target"` fallback also extracted.

**Migration sites (24 frontend files touched):**
- New: `web/src/components/{FormField,Select,formFieldContext}.{tsx,ts}` + `web/src/test/setup.ts` + `web/src/{lib/cn.test.ts,components/{Button,FormField}.test.tsx}`.
- Modified: `web/src/lib/cn.ts`; `web/src/theme/{tokens.css,ThemeToggle.tsx}`; `web/src/components/{Button,Banner,TargetTile,DestructiveConfirm,TargetDetails,RecentEventsMenu,LogViewer,LiveStatsStrip,AppShell}.tsx`; `web/src/components/settings/SettingsSidebar.tsx`; `web/src/pages/settings/{GeneralTab,SessionsTab,SecurityTab}.tsx`; `web/src/pages/settings/targets/{VKTab,PersistentTargetTab,CustomTab}.tsx`; `web/src/messages.ts`; `web/{vite.config.ts,eslint.config.js,package.json,package-lock.json}`.

**Misc closeouts:**
- `package.json` `browserslist` floor (Chrome ≥111 / Firefox ≥113 / Safari ≥16.4 / Edge ≥111) — matches Tailwind v4 minimum, closes FH2-L1.
- SettingsSidebar inline comment naming the section-header invariant as the load-bearing tier signal (UX-B.2).
- PersistentTargetTab's "__custom__" sentinel branch: custom-URL Input renders OUTSIDE the FormField (HTML disallows two controls under one `htmlFor`). Input has its own `aria-label` + placeholder.

**Rule №4 code-reviewer pass:** Agent `aefd3416adc3d3fd3`. **2 High + 3 Medium findings — ALL FIXED in-slice:**
- **H-1 (AccountMenu trigger)** — trigger Button was not marked `iconOnly`, so it picked up the `text-button` size path (px-5 + gap-2 + reserved 14px spinner gutter). Spinner gutter is dead weight on a Radix trigger that never loads. Fixed: added `iconOnly` to the Button trigger; intra-icon gap reduced to `gap-(--space-1)`; chip aesthetic preserved via `rounded-full`.
- **H-2 (FormField.Control / SecretField label association)** — `CustomTab`'s `<FormField><FormField.Control>{() => <SecretField/>}</FormField.Control></FormField>` rendered a `<label htmlFor={generated-id}>` that pointed at no DOM node (SecretField owns its own input id). Fixed: removed FormField wrapper for the SecretField field; restored manual `<div><span>label</span><SecretField/></div>` pattern (SecretField's `ariaLabel` handles SR announcement).
- **M-1 (Select caret inline-color override)** — `caretStyle()` set `color: var(--color-fg-muted)` inline, which has higher specificity than the Tailwind `disabled:text-(--color-fg-disabled)` utility — caret color was stuck regardless of disabled state. Fixed: removed the inline `color` property; caret inherits from `text-*` utility classes via SVG `currentColor`.
- **M-2 (RecentEventsMenu aria-haspopup mismatch)** — `aria-haspopup="menu"` lies to ATs: the popup is a custom div, not a `role="menu"` container with `menuitem` children. Fixed: changed to `aria-haspopup="dialog"` (accurate for the panel-with-list-and-clear-button shape). Full Radix DropdownMenu migration deferred to slice 10.
- **M-3 (Radix DropdownMenu missing from manualChunks)** — `@radix-ui/react-dropdown-menu` was not in `vite.config.ts`'s `manualChunks.radix` array; Rollup placed it in the main bundle. Fixed: added to the array. Bundle re-balanced: main index chunk 127 KB (was 132 KB), radix chunk 28 KB (was 24 KB). Net bundle 232 KB (was 234 KB) — better cacheability.

**Gates at slice 6 close (post-reviewer fixes):**
- frontend typecheck: green
- frontend lint: green (`--max-warnings 0`)
- frontend vitest: **37/37** (was 22 — +15 new across cn / FormField / Button suites)
- frontend build: 232 KB gzipped (budget 256 KB)
- backend pytest: **723/723** (unchanged — zero backend touch)
- backend mypy: **71/71** (unchanged)

**Items DEFERRED to slice 10** (not in slice-6 scope):
- UX-F10 — auth-page first-focus on password (not primitive work; needs autoFocus rework).
- UX-F11 — SettingsSidebar mobile horizontal scroll (responsive layout change; needs its own design pass).
- UI-F8 — Sparkline empty-state visibility (not in primitive rebuild).

---

## Slice 7 — Phase 4 (2026-05-19)

**Scope:** Auth/observability hardening. 8 findings closed: **BA-F3, BA-F5, BA-F6, BA-F8 + FG2-H1, FG2-H5, FG2-M4, FG2-M7.** Backend-only diff; zero frontend touch. The whole Phase 4 collapsed into a single slice — these findings cluster naturally around two new modules (`LastSeenCoalescer`, AAD v2 helpers) plus surgical regex/ordering fixes.

**Design phase (Rule №3):** backend-architect (engineering-backend-architect prompt) consulted in parallel on 5 adjudication points: (1) reprompt rate-limit shape, (2) AEAD v2 AAD shape + migration story, (3) last_seen coalescing window + placement, (4) redaction regex shape + URL/buf caps, (5) audit-before-unlink ordering. All verdicts adopted as written; one clarification — chose **401 + Retry-After** (matches login's failure-mode-unification) over the architect's initial 429 suggestion, with the architect's later "match login's shape" rationale supporting either.

**New module — `app/auth/last_seen_coalescer.py`** (Hex Audit BA-F6 + FG2-H1 + FG2-M7):
- `LastSeenCoalescer` class with `should_write(token_hash) -> bool` / `mark_written(token_hash)` / `forget(token_hash)` / `reset()` API. Two-step contract (gate → caller-writes → mark) so a failed/timed-out write doesn't over-mark.
- Constants: `LAST_SEEN_COALESCE_WINDOW_SECONDS = 60.0` (collapses normal SPA page-load's ~10-20 verify_cookie calls to one DB write), `MAX_TRACKED_SESSIONS = 10_000` (FIFO eviction mirrors `LoginRateLimiter.MAX_TRACKED_BUCKETS`), `LAST_SEEN_TOUCH_TIMEOUT_SECONDS = 2.0` (asyncio.wait_for timeout).
- Single-event-loop invariant documented (workers=1; multi-worker would need a different design).
- 14 unit tests in `tests/auth/test_last_seen_coalescer.py` covering window expiry, FIFO eviction, MRU-promotion on re-mark, `forget`, type validation.

**`SessionAuthService.verify_cookie` + WS upgrade** wired to call the coalescer:
- HTTP path (sessions.py): if `should_write` → `asyncio.wait_for(touch_last_seen, timeout=2.0)` → `mark_written`. On TimeoutError: log warning, proceed.
- WS path (ws.py): same shape, with explicit `await session.rollback()` in the timeout branch (reviewer H-2 fix — ensures the connection returns to the pool clean even though `AsyncSession.__aexit__` rolls back implicitly).
- `revoke` calls `coalescer.forget(token_hash)`.
- `revoke_all_for_user` deliberately does NOT forget (we only have user_id, not token_hashes); stale entries decay via FIFO eviction.

**AEAD AAD v2** (Hex Audit BA-F5):
- `AAD_CREDENTIAL_STREAM_KEY_V2 = b"credential:stream_key:v2"` + new `build_credential_aad_v2(salt, target_id)` — AAD = `discriminator + salt + target_id_utf8`. Salt's fixed width delimits target_id unambiguously.
- `encrypt_credential(key, plaintext, salt, target_id)` — always writes v2 AAD.
- `decrypt_credential(key, nonce, ct, salt, target_id)` — tries v2, falls back to v1 on `AEADDecryptionError`. Rolling migration: legacy v1 rows stay readable; the next write (set_credential or rotate-passphrase) upgrades them to v2 automatically. NO schema column (architect verdict — Rule №2 friendly).
- 5 call sites swapped: `targets.set_credential`, `targets.reveal_credential`, `security_api.rotate-passphrase` decrypt+encrypt loop, `supervisor._load_runnable_targets`, `supervisor._reload_target`.
- Cross-row substitution attack closed for v2 rows: copying `(ciphertext, nonce, salt)` between target rows fails AEAD because v2 AAD binds target_id. v1 rows remain vulnerable until next write — accepted in architect verdict (rolling migration baseline).
- 7 new tests in `tests/crypto/test_aead.py` (`TestBuildCredentialAadV2` + `TestCredentialAeadHelpers`): v2 round-trip, wrong-target-id fails, v1 legacy fallback, v2-not-decryptable-with-v1, v1-not-decryptable-with-v2, v1 fallback permissive on target_id, tampered-v2 raises (reviewer M-2 fix).

**Reprompt rate-limit + timing floor** (Hex Audit BA-F3):
- `AuthState.reprompt_rate_limiter = LoginRateLimiter(failure_limit=3, window_seconds=900.0)` — tighter than login's 5 (user is already authenticated; threat is a stolen cookie brute-forcing the password from inside the session, smaller blast radius beats login's headroom).
- `REPROMPT_TIMING_FLOOR_SECONDS = 0.30` mirrors `CHANGE_PASSWORD_TIMING_FLOOR_SECONDS` — both paths run Argon2 verify, the difference between success (verify + issue) and failure (verify only) is sub-ms, the 300 ms floor erases any residual signal.
- Failure mode: **401 + Retry-After** for rate-limited (matches login's `RATE_LIMITED.value` detail + Retry-After header pattern, not 429 — the body shape unifies with wrong-password so a stolen-cookie attacker cannot distinguish rate-limit from wrong-password by status code alone).
- 3 new tests in `tests/api/test_auth.py`: pre-charged limiter denies even-correct password, 3 wrong-password attempts lock out, wrong-password padded to floor.

**Redaction regex tightening + URL/buf caps** (Hex Audit BA-F8 + FG2-M4):
- `_RTMP_PATH_KEY_RE` host/app segments now stop at `?` and `#` (was `[^/\s]+` → now `[^/\s?#]+`) so a trailing query string never extends the "app" capture.
- NEW `_RTMP_QUERY_KEY_RE` catches query-string credential leaks. Reviewer M-1 fix: explicit param-name allowlist (`key`, `streamkey`, `stream_key`, `streamkeyid`, `auth_key`, `authkey`, `apikey`, `api_key`, `secret`, `token`, `signature`) instead of an open-ended `*key*` substring match (which over-matched `?keyboard=`, `?monkey=`, etc.). Case-insensitive. Captures ≥8-char values only.
- `MAX_WORKER_URL_LEN = 2048` ValueError in constructor — refuses an absurd worker_url (programming bug upstream).
- `MAX_BUF_LEN = 4 * MAX_LINE_LEN = 16 384` runtime cap (reviewer H-1 fix: cap reordered to be checked BEFORE `force_split_threshold` so the two caps are independent — the original ordering made the second cap dead code given `force_split_threshold ≤ 6144 < MAX_BUF_LEN`).
- 9 new tests in `tests/fanout/test_redaction.py`: query-string `streamKey=` / `auth_key=` / `STREAMKEY=` redacted, short values + non-key param names NOT redacted, path doesn't swallow query string, URL cap accepted at boundary + rejected one over, no-newline runaway is bounded.

**KDF-salt audit-before-unlink ordering** (Hex Audit FG2-H5):
- Extracted `_kdf_salt_previous_cleanup_once(typed_path, sessionmaker)` from the inline loop so the invariant is unit-testable (per Rule №1 — the inline body was untestable, not a fake-fix shortcut). Loop becomes `sleep` + delegate.
- Sequence: stat → if aged > grace → audit `kdf_salt_previous_cleared` (with `path` + `mtime` in data) BEFORE unlink → unlink. Audit failure: skip unlink, next tick retries (mtime is unchanged, file still there). Audit succeeds + unlink fails: next tick re-audits (duplicates self-describing — same `mtime` field).
- 4 new tests in `tests/test_kdf_salt_previous_cleanup.py`: happy path, audit-failure-skips-unlink, within-grace no-op, missing-file no-op.

**Test fixture migration:** all `AuthState()` constructors (6 in `tests/api/conftest.py`, `tests/auth/test_deps.py`, `tests/api/test_phase9_endpoints.py`) gained `reprompt_rate_limiter` + `last_seen_coalescer` args. All `SessionAuthService()` constructors (7 in `tests/auth/test_sessions.py`) gained `last_seen_coalescer` — added a shared `coalescer` fixture to keep the test bodies short. Two `aead_decrypt(...build_credential_aad(salt))` sites in `tests/api/test_phase9_endpoints.py` swapped to `decrypt_credential(...salt, tid)` for post-rotation verification.

**Files changed in slice 7 (16 backend files):**
- New: `app/auth/last_seen_coalescer.py`, `tests/auth/test_last_seen_coalescer.py`, `tests/test_kdf_salt_previous_cleanup.py`.
- Modified app/: `app/auth/{deps,sessions}.py`, `app/api/{auth,ws,targets,security_api}.py`, `app/crypto/{aead,__init__}.py`, `app/fanout/{redaction,supervisor}.py`, `app/main.py`.
- Modified tests/: `tests/api/{conftest,test_auth,test_phase9_endpoints}.py`, `tests/auth/{test_deps,test_sessions}.py`, `tests/crypto/test_aead.py`, `tests/fanout/test_redaction.py`.

**Rule №4 code-reviewer pass:** Agent `a3c446bd2e48083a6`. **2 High + 3 Medium findings — ALL FIXED in-slice:**

1. **H-1 — `MAX_BUF_LEN` second-cap was dead code.** The original ordering checked `len(buf) < force_split_threshold` first then `len(buf) <= MAX_BUF_LEN` — but `force_split_threshold ≤ 6144 < MAX_BUF_LEN = 16384`, so the inner branch was unreachable. Fixed: reordered to check `MAX_BUF_LEN` FIRST so the two caps are independent.
2. **H-2 — WS path swallowed `TimeoutError` without explicit rollback.** `asyncio.wait_for` cancels the in-flight UPDATE mid-execution; the AsyncSession holds an open transaction. `__aexit__` rolls back on close, but the explicit `await session.rollback()` documents the intent and removes any ambiguity around partially-applied state.
3. **M-1 — `_RTMP_QUERY_KEY_RE` over-matched `?keyboard=` / `?monkey=`.** Substring `key` matched many non-credential param names. Fixed: switched to an explicit param-name allowlist (`streamKey`, `stream_key`, `auth_key`, `apikey`, `secret`, `token`, `signature`, etc.) instead of the open `*key*` pattern.
4. **M-2 — Missing test for tampered v2 ciphertext.** Pinned: a v2 row with a flipped ciphertext byte must raise `AEADDecryptionError` (not silently fall back to v1 — both v2 and v1 AAD attempts fail because AES-GCM's tag verification fails regardless of AAD).
5. **M-3 — `test_reprompt_rate_limited_after_threshold` missing `reset()`.** Sibling tests called `reset()` explicitly; this one relied on fixture isolation. Added the `reset()` for consistency.

**Gates at slice 7 close (post-reviewer fixes):**
- backend mypy: **72/72** (was 71 — +1 for `last_seen_coalescer.py`)
- backend pytest: **767/767** (was 723 — +44 across 4 new/extended test files)
- frontend typecheck / lint / **37 vitest** / build **232 KB** gzipped — unchanged (zero frontend touch)

**Locking discipline:** No supervisor / rotation_lock changes in slice 7. Slice-3 invariant (supervisor._lock BEFORE rotation_lock) untouched.

---

## Recovering truncated audit transcripts

The 2026-05-18 baseline run's prior-session transcript files at the path below are zero-byte (lost on session end). Future hex-audits should persist findings into the burndown file *immediately* on receipt to avoid this loss-on-truncation problem.

- `C:\Users\Alexgrv\AppData\Local\Temp\claude\d--Projects-Programming-Restream-Plus\<session-id>\tasks\<agent-id>.output`

| Agent | ID | Status |
|---|---|---|
| Backend Architect | `ae36077f705b3d49c` | Full text was in original session context — captured into this burndown |
| Software Architect | `a82941f78afc5bbac` | Full text was in original session context — captured into this burndown |
| UX Architect | `afd7bac2940b1d8f9` | Full text was in original session context — captured into this burndown |
| UI Designer | `a15eb375a7bdfb986` | Full text was in original session context — captured into this burndown |
| Code-reviewer (orig) | `adedabb6fc991a557` | Transcript file = 0 bytes; F1-F6 LOST. Fresh re-run scheduled. |
| Footgun Hunter (orig) | `aefec5b846061ebff` | Transcript file = 0 bytes; F6-F12 LOST. Fresh re-run scheduled. |
| Footgun Hunter (re-run 2026-05-18) | `aba08162de9691354` | Fresh pass on slice-1 working tree — recovers F6-F12-class |
| Code-reviewer (re-run 2026-05-18) | `ad4b45152ea2d01e4` | Fresh pass on slice-1 working tree — recovers CR-F1-F6-class |
| Footgun Hunter | `aefec5b846061ebff` | F13-F17 + summary in context; F1-F12 partial (F1-F5 recovered via re-query) — recover F6-F12 via SendMessage |

---

## Slice 8 — Phase 5 COMPLETE (2026-05-19)

The last Critical (BA-F4) closed. Surgical backend slice — transactional audit-log refactor, no frontend touch.

### Closed audit findings (slice 8)

**1 Backend Architect Critical:**
- BA-F4 — `AuditLogRepository` rewritten with dual contract. `append(session, ...)` adds the audit row to the caller's `AsyncSession` BEFORE commit (in-txn); `append_standalone(...)` opens its own short-lived session, reserved for paths with no caller session (lifespan startup + supervisor lifecycle + kdf-salt cleanup). New `delete_older_than(session, *, cutoff)` and `run_wal_checkpoint(*, reason)` drive the new retention loop. ALL 13 request-scoped sites refactored: audit append BEFORE `session.commit()`, every `contextlib.suppress(Exception)` wrapping an audit call REMOVED. Per-append `wal_checkpoint(TRUNCATE)` removed from the in-txn path (it can't run inside the caller's commit ordering) — replaced by the periodic retention loop.

**1 Backend Architect Medium:**
- BA-F11 — `revoke_http_session` audit moved INSIDE the business txn alongside `repo.delete(token_hash)`. ONE txn, both writes commit atomically, duplicate-audit window closed.

**1 Footgun Hunter Medium:**
- FG2-M6 — `_audit_log_retention_loop` (6 h cadence) deletes `at < now - audit_log_retention_days`. Default 365 days, configurable via `RESTREAM_AUDIT_LOG_RETENTION_DAYS`, floor 30 / ceiling 3650. Non-empty ticks emit an `audit_log_retention_run` audit-of-the-audit row in the SAME txn as the delete, then run `wal_checkpoint(TRUNCATE)`. Idle ticks checkpoint at most once per day. Loop is also the periodic WAL-checkpoint driver — kills two findings in one loop.

**1 Software Architect Medium (docs):**
- SA-F4 — ADR-0007 §"Audit log scope" amended: explicit note that targets/settings CRUD IS audited (`target_created/updated/deleted`, `settings_updated`) contrary to the original ADR text. Also added the new "Audit-log durability and transactional shape" section under `synchronous=NORMAL` documenting the BA-F4 fix.

### Cascading bug-fixes embedded in the refactor (not separately tracked)

- `password_changed` (auth.py:296-306) — the "commit BEFORE audit so its own session doesn't deadlock on the SQLite write lock" comment was the BA-F4 bug documented in code. Removed; one writer, one txn.
- `_wipe_per_session_credentials` (supervisor.py:842-865) — the two-phase "wipe txn then audit" pattern with an explicit "running it inside the wipe txn would deadlock" comment was the BA-F4 bug. Replaced by one `session.begin()` block doing both deletes and audit appends.
- `_ingest_key_previous_cleanup_loop` (main.py:641-679) — the two-session pattern (one for the clear, one for the audit) was the BA-F4 bug. Merged into one `session.begin()` block.
- `app_started` lifespan emit (main.py:374-381) — `contextlib.suppress(Exception)` REMOVED. A boot-time audit failure is now fatal: lifespan raises, FastAPI startup aborts, uvicorn exits non-zero. Operator sees the broken DB immediately, not at the next forensic investigation.

### Design phase (Rule №3)

engineering-backend-architect (agent `a7ad73912eb358c6a`) consulted on 10 adjudication points: contract shape, WAL checkpoint timing, BA-F11 duplicate-audit window closure, password_changed ordering, passphrase_rotated merging, background-loop pattern, app_started failure semantic, supervisor `_audit` helper, retention policy shape (including `audit_log_retention_days` default + bounds), failure semantic across the 13 request sites. Verdict adopted as written.

### New files

- `tests/db/test_audit_log_retention.py` (4 tests) — retention loop invariants (delete + marker atomic, empty-tick silent, marker rolls back with caller, WAL checkpoint runs).

### Modified files (10 app/ + 3 tests/ + 1 ADR)

**App:**
- `app/repositories/audit_log.py` — full rewrite (200 → 240 lines)
- `app/config.py` — new `audit_log_retention_days` field
- `app/api/targets.py` — 6 call sites refactored
- `app/api/settings_api.py` — 3 call sites refactored
- `app/api/security_api.py` — 2 call sites refactored (passphrase_rotated moved INSIDE the credential-rewrap session.begin block; session_revoked moved before commit)
- `app/api/auth.py` — 2 call sites refactored (password_rehashed merged into same session.begin; password_changed audit-before-commit + drop suppress)
- `app/api/api_tokens_api.py` — 2 call sites refactored
- `app/fanout/supervisor.py` — `_audit` helper now calls `append_standalone`; `_wipe_per_session_credentials` merged into one txn
- `app/main.py` — `app_started` drop suppress + switch to `append_standalone`; `_kdf_salt_previous_cleanup_once` switch to `append_standalone`; `_ingest_key_previous_cleanup_loop` merged into one txn; new `_audit_log_retention_loop`; new `AUDIT_LOG_RETENTION_CHECK_INTERVAL_SECONDS` constant

**Tests:**
- `tests/db/test_repositories.py::TestAuditLogRepository` — class rewritten (3 → 7 tests)
- `tests/db/test_audit_log_retention.py` — NEW (4 tests)
- `tests/test_kdf_salt_previous_cleanup.py` — `_FakeAuditRepo.append` → `append_standalone`
- `tests/api/test_phase9_endpoints.py` — 2 direct-seeding sites switched to `append_standalone`

**Docs:**
- `docs/architecture/ADR-0007-persistence.md` — new "Audit-log durability and transactional shape" section + audit-scope correction (SA-F4)

### Rule №4 code-reviewer pass

Agent `aa6dd9841070ce64e` reviewed the full slice-8 diff against 10 specified checklist items (atomicity, FK on target_deleted, retention loop ordering, idempotency of ingest-key loop, wipe txn bound, app_started fatal semantic, validation floors, lock-ordering invariant preservation, prior-slice fix preservation, hidden races). **Result: ZERO findings — all 10 checklist items clear.** No in-slice fixes required. The reviewer's only non-finding observation: `AuditLogRepository` instance constructed once outside the retention loop and reused across iterations is safe (confidence below 80 — mentioned only for completeness).

### Gates at slice 8 close

- backend mypy: **72/72** (unchanged — repo rewrite + 1 new module function, no new modules)
- backend pytest: **775/775** (was 767 — +4 audit-log repo contract tests + +4 retention-loop invariant tests)
- frontend typecheck/lint/**37 vitest**/build **232 KB** gzipped — unchanged (zero frontend touch)

### Burndown after slice 8

- **~64/100 findings closed (~64 %)**
- **16/16 Critical drained (100 %)** — all original Criticals now closed
- Phase 1 COMPLETE (3/3), Phase 2 COMPLETE (2/2), Phase 3 COMPLETE (1/1 oversized slice), Phase 4 COMPLETE (1/1), **Phase 5 COMPLETE (1/1 — slice 8 closed BA-F4 + BA-F11 + FG2-M6 + SA-F4)**

### Next: Re-audit Checkpoint #2 (MANDATORY)

Per the phase table, Phases 2 and 5 are the two checkpoint slots. Slice 8 is the Phase-5 tail edge — a fresh hex-audit run on the cumulative Phase 5 diff is required before Phase 6 work can begin. Scope:
- 5-lens hex-audit (backend-architect, software-architect, ux-architect, ui-designer, code-reviewer)
- Rule №6 footgun-hunter pass on the new retention loop + transactional refactor surface
- Any High/Critical findings get a slice 8.5 fix-up; Medium/Low join the slice-10 cleanup pile

### Phase 6 ahead (slices 9-10)

- **Slice 9** — ADR documentation pass: SA-F1, F2, F5-F9, F11-F13 (8 docs items).
- **Slice 10** — Medium cleanup pile: BA-F9/F13/F15/F19, FG-F16, FG2-H2/H4/M2/M5/L2, UX-F10/F11/F12/F14, UI-F8/F14, SA-F14, CR-F8-F12. Plus 5 slice-6 deferrals.
