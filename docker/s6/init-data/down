#!/command/execlineb -P
# init-data is idempotent; nothing to undo on shutdown.
# Like `up`, s6-rc parses this as execline — `exit 0` would try to
# exec a binary literally named `exit`. /bin/true is the canonical
# no-op.
/bin/true
